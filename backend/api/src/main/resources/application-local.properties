# =============================================================================
# Swing Trade Application - Local Development Configuration
# =============================================================================
# This file contains development-specific settings for local machine
# Use environment variables for sensitive data in production
# =============================================================================

# =============================================================================
# Server Configuration (Local)
# =============================================================================
server.port=8080

# Bounded repair is opt-in even in local development.
ingestion.reconcile.apply.enabled=${INGESTION_RECONCILE_APPLY_ENABLED:false}
reconcile.apply.token=${RECONCILE_APPLY_TOKEN:}

# Disable DevTools for now (causes dialect detection issues)
spring.devtools.restart.enabled=false

# =============================================================================
# Database Configuration (Dev Stack - PostgreSQL on pi-node)
# =============================================================================
# Connect to PostgreSQL running on pi-node via docker-compose.infra.yml
spring.datasource.url=jdbc:postgresql://192.168.0.100:5435/swingtrade_db
spring.datasource.username=swingtrade_user
spring.datasource.password=swingtrade_password

# Use development connection pool settings
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.connection-timeout=10000

# Enable SQL logging in development
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.highlight_sql=true

# Allow schema updates for development (use with caution!)
spring.jpa.hibernate.ddl-auto=update

# Hibernate 7 auto-detects dialect — explicit setting removed

# =============================================================================
# Upstox API Configuration (Development/Sandbox)
# =============================================================================
# Set to false for development - no real trading
upstox.api.base-url=https://api.upstox.com
upstox.client-id=${UPSTOX_CLIENT_ID:mock_client_id}
upstox.client-secret=${UPSTOX_CLIENT_SECRET:mock_client_secret}
upstox.redirect-uri=${UPSTOX_REDIRECT_URI:http://localhost:8080/auth/upstox/callback}
upstox.access-token=${UPSTOX_ACCESS_TOKEN:}
upstox.token-store-path=${UPSTOX_TOKEN_STORE_PATH:data/upstox-tokens.json}

# Development mode - disable actual trading
trading.enabled=false
trading.paper.trading.enabled=true
real.trading.enabled=false

# =============================================================================
# LLM / vLLM Configuration (Local Development)
# =============================================================================
# Spring AI 2.0.1 — OpenAI-compatible server (vLLM) via OkHttp
spring.ai.openai.base-url=http://localhost:8000
spring.ai.openai.chat.model=${LLM_MODEL_NAME:claude-sonnet-4-6}
spring.ai.openai.chat.temperature=0.0
spring.ai.openai.timeout=30s
spring.ai.openai.chat.max-tokens=4096
llm.backend=pi_ssh
llm.base-url=${LLM_BASE_URL:http://piworm.local:8090/v1}

# Dummy key — Spring AI's OpenAI auto-configuration requires *some* credential to construct
# its client bean even though this app doesn't use OpenAI's hosted API (uses local vLLM/gpuhub instead)
spring.ai.openai.api-key=${OPENAI_API_KEY:not-needed}

# Dummy key — Spring AI's OpenAI auto-configuration requires *some* credential to construct
# its client bean even though this app doesn't use OpenAI's hosted API (uses local vLLM/gpuhub instead)
spring.ai.openai.api-key=${OPENAI_API_KEY:not-needed}

# Disable LLM in development if not running
llm.enabled=${LLM_ENABLED:false}

# =============================================================================
# Telegram Bot Configuration (Development)
# =============================================================================
# Enable/disable Telegram notifications
telegram.bot.enabled=${TELEGRAM_BOT_ENABLED:false}
telegram.bot.token=${TELEGRAM_BOT_TOKEN:}
telegram.bot.username=${TELEGRAM_BOT_USERNAME:}

# Disable notifications in local development by default
telegram.notify.on.trades=false
telegram.notify.on.signals=false
telegram.notify.on.errors=true
telegram.notify.on.positions=false

# =============================================================================
# Trading Configuration (Development Mode)
# =============================================================================
# Paper trading only in development
trading.enabled=false
trading.paper.trading.enabled=true
trading.simulation.mode=true

# Conservative position limits for development
trading.max.position.size.percentage=5
trading.max.exposure.percentage=25
trading.stop.loss.percentage=3.0
trading.take.profit.percentage=10.0
trading.min.signal.confidence=70.0

# =============================================================================
# Strategy Configuration (Development)
# =============================================================================
# Conservative strategy settings for development
strategy.enabled=true
strategy.decision.threshold=70
strategy.max.open.positions=3

# =============================================================================
# Logging Configuration (Development)
# =============================================================================
# Verbose logging for development
logging.level.root=INFO
logging.level.com.swingtrade=DEBUG
logging.level.com.swingtrade.api=DEBUG
logging.level.com.swingtrade.data=DEBUG
logging.level.com.swingtrade.strategy=DEBUG
logging.level.com.swingtrade.llm=DEBUG
logging.level.com.swingtrade.broker=DEBUG
logging.level.com.swingtrade.core=DEBUG

# SQL logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
logging.level.org.flywaydb=DEBUG

# Console output with color support (if terminal supports it)
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n

# Logging — logback-spring.xml handles file appender via LOG_FILE env var
# Also set logging.file.name for AOT processor (which has its own log config)
logging.file.name=./logs/swing-trade-local.log

# Human-readable console pattern for local dev
CONSOLE_LOG_PATTERN=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# =============================================================================
# Actuator / Health Check Configuration (Development)
# =============================================================================
# Expose more actuator endpoints in development
management.endpoints.web.exposure.include=health,info,metrics,prometheus,env,configprops,threaddump
management.endpoints.web.exposure.exclude=
management.endpoint.health.show-details=always
management.endpoint.health.probes.enabled=true

# =============================================================================
# Security Configuration (Development)
# =============================================================================
# Disable CSRF for development API testing
spring.security.filter.enabled=false

# Development CORS settings
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000
spring.web.cors.allowed-credentials=true

# =============================================================================
# Yahoo Finance Configuration
# =============================================================================
yahoo.finance.base-url=https://query1.finance.yahoo.com
yahoo.finance.rate-limit-ms=500
yahoo.finance.user-agent=Mozilla/5.0
market-data.rate-limit-ms=${MARKET_DATA_RATE_LIMIT_MS:250}
data.backfill.parallelism=${DATA_BACKFILL_PARALLELISM:5}
data.backfill.chunk-days=${DATA_BACKFILL_CHUNK_DAYS:30}
data.rate-limit.upstox.requests-per-minute=${DATA_RATE_LIMIT_UPSTOX_RPM:100}
data.rate-limit.fyers.requests-per-minute=${DATA_RATE_LIMIT_FYERS_RPM:60}
data.rate-limit.default.requests-per-minute=${DATA_RATE_LIMIT_DEFAULT_RPM:30}
ingestion.default-years=3

# =============================================================================
# Discord Webhook (Development)
# =============================================================================
discord.webhook.url=
discord.webhook.enabled=false

# =============================================================================
# PDF Extraction (Development)
# =============================================================================
llm.pdf.base-url=
llm.pdf.model=gemma-4-E2B

# =============================================================================
# Feature Flags (Development)
# =============================================================================
app.features.data-ingestion.enabled=true
app.features.strategy.enabled=true
app.features.sentiment-analysis.enabled=true
app.features.paper-trading.enabled=true
app.features.real-trading.enabled=false
app.features.scheduler.enabled=true
app.features.candidate-scan.scheduler.enabled=true

# =============================================================================
# Job Orchestrator
# =============================================================================
job.orchestrator.max-concurrent=3
job.orchestrator.poll-interval-ms=3000
job.orchestrator.reaper.enabled=true
job.orchestrator.reaper.initial-delay-ms=15000
job.orchestrator.reaper.interval-ms=300000
job.orchestrator.llm-analysis.enabled=${LLM_ANALYSIS_ENABLED:true}
job.orchestrator.llm-analysis.advisory-only=${LLM_ANALYSIS_ADVISORY_ONLY:true}

# =============================================================================
# Database Initialization for Development
# =============================================================================
# Create database if not exists (PostgreSQL specific)
spring.sql.init.mode=never

# Flyway migrations for local development
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.validate-on-migrate=false

# =============================================================================
# Testing Configuration
# =============================================================================
# Test profile settings
spring.test.mockmvc=false

# =============================================================================
# Additional Development Utilities
# =============================================================================
# Enable OpenAPI documentation
springdoc.api-docs.enabled=true
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true

# =============================================================================
# Fyers API Configuration
# =============================================================================
# Credentials loaded from .env via environment variables
# Runtime broker selection via /api/settings/broker (stored in DB)

fyers.clientId=${FYERS_CLIENT_ID:}
fyers.secretKey=${FYERS_SECRET_KEY:}
fyers.redirectUrl=${FYERS_REDIRECT_URL:http://127.0.0.1:8080/api/fyers/callback}
fyers.pin=${FYERS_PIN:}
fyers.accessToken=
fyers.refreshToken=
fyers.frontend-url=http://localhost:3003

# =============================================================================
# Note for Developers
# =============================================================================
# ================================================================
# LOCAL DEVELOPMENT ONLY - DO NOT USE IN PRODUCTION
# ================================================================
#
# Remember to:
# 1. Use environment variables for sensitive credentials
# 2. Disable paper trading when testing with real data
# 3. Use conservative trading limits
# 4. Monitor database connections carefully
# 5. Keep detailed logs for debugging
#
# To use production settings, create application-prod.properties
# or set the SPRING_PROFILES_ACTIVE environment variable
# ================================================================
