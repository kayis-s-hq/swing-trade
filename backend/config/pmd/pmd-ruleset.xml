<?xml version="1.0"?>
<ruleset name="swing-trade"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">
  <description>Swing Trade PMD rules — PMD 7.x (errorprone + security + multithreading only)</description>

  <!-- Keep only errorprone — real bugs, noisy rules excluded -->
  <rule ref="category/java/errorprone.xml">
    <exclude name="AvoidDuplicateLiterals"/>
    <exclude name="AvoidLiteralsInIfCondition"/>
    <exclude name="NullAssignment"/>
    <exclude name="TestClassWithoutTestCases"/>
    <exclude name="MissingSerialVersionUID"/>
    <exclude name="AvoidFieldNameMatchingMethodName"/>
    <exclude name="UseConcurrentHashMap"/>
    <exclude name="DoNotUseThreads"/>
  </rule>

  <!-- Keep only security — critical -->
  <rule ref="category/java/security.xml"/>

  <!-- Keep only multithreading — concurrency bugs -->
  <rule ref="category/java/multithreading.xml">
    <exclude name="AvoidSynchronizedStatement"/>
    <exclude name="AvoidSynchronizedAtMethodLevel"/>
    <exclude name="UseConcurrentHashMap"/>
    <exclude name="DoNotUseThreads"/>
  </rule>
</ruleset>