---
phase: 05-api-layer
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/main/java/com/swingtrade/api/dto/ScanResponse.java
  - api/src/main/java/com/swingtrade/api/service/ScanService.java
autonomous: true
requirements:
  - REQ-024
user_setup: []

# WORKTREE WORKFLOW ENFORCEMENT
# CRITICAL: This plan MUST be executed in a git worktree environment
# Root .planning/ is source of truth - worktree .planning/ is working copy
worktree_enforcement:
  required: true
  reason: "Prevents direct edits to root .planning/ on main branch"
  workflow:
    - step: 1
      action: "Verify worktree directory"
      command: "pwd | grep worktrees"
      fail_message: "ERROR: Must be in a worktree directory (e.g., .claude/worktrees/phase-05/)"
    - step: 2
      action: "Verify worktree branch"
      command: "git branch --show-current"
      expected_pattern: "worktree-phase-.*"
      fail_message: "ERROR: Must be on a worktree branch (e.g., worktree-phase-05)"
    - step: 3
      action: "Edit planning docs in worktree"
      path: ".planning/phases/05-api-layer/"
      note: "Do NOT edit .planning/ in root repository"
    - step: 4
      action: "Sync to root before merge"
      command: "Skill(\"superpowers:gsd-worktree-workflow --sync-to-root\")"
      when: "Before merging worktree branch to main"
    - step: 5
      action: "Verify before merge"
      command: "gsd:verify"
      when: "After plan completion, before merge"
user_setup: []
must_haves:
  truths:
    - POST /api/scan returns ScanResponse with symbolsScanned count and symbols list
    - No duplicate field declarations in ScanResponse
    - ScanService uses correct setter name for symbolsScanned
  artifacts:
    - path: api/src/main/java/com/swingtrade/api/dto/ScanResponse.java
      provides: "Scan response DTO without duplicate fields"
      min_lines: 100
    - path: api/src/main/java/com/swingtrade/api/service/ScanService.java
      provides: "Manual scan trigger using existing SignalEngine API"
      exports: ["triggerManualScan"]
  key_links:
    - from: api/src/main/java/com/swingtrade/api/service/ScanService.java
      to: strategy/src/main/java/com/swingtrade/strategy/SignalEngine.java
      via: "generateSignalForSymbolNow"
      pattern: "signalEngine\\.generateSignalForSymbolNow"
---

<objective>
Fix ScanResponse duplicate field and SignalEngine API mismatch for /api/scan endpoint.

Purpose: Resolve blocker gaps preventing /api/scan from compiling and returning correct response.

Output: Fixed ScanResponse.java and ScanService.java that compiles without errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-api-layer/05-UAT.md

# ScanResponse - Duplicate Field Issue
Gap 1: Line 14 has `Integer symbolsScanned;` and Line 19 has `List<String> symbolsScanned;` - same field name with different types.

# SignalEngine API
Gap 8, 9: SignalEngine does NOT have `generateSignal(String)` - only has `generateSignalForSymbolNow(String)`

# ScanService Usage
Gap 7: ScanService may call non-existent `setSymbolsScannedCount()` - need to use `setSymbolsScanned()`

</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ScanResponse duplicate symbolsScanned field</name>
  <files>api/src/main/java/com/swingtrade/api/dto/ScanResponse.java</files>
  <action>
Remove duplicate field declaration. The ScanResponse has TWO symbolsScanned fields:
- Line 14: `private Integer symbolsScanned;` (count of symbols scanned)
- Line 19: `private List<String> symbolsScanned;` (list of symbol names)

Keep BOTH but rename the List version to `scannedSymbols` to avoid collision:
- Line 14: Keep `private Integer symbolsScanned;` as count
- Line 19: Rename to `private List<String> scannedSymbols;` and update getter/setter accordingly

This matches the pattern in ScanResult class where `opportunityCount` and `opportunities` are used.
</action>
  <verify>
<automated>cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl api -q 2>&1 | grep -i "ScanResponse" || echo "No ScanResponse errors"</automated>
  </verify>
  <done>
ScanResponse.java compiles without duplicate field errors. Field `symbolsScanned` returns Integer count, new field `scannedSymbols` returns List<String>.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix ScanService to use correct SignalEngine API and setter</name>
  <files>api/src/main/java/com/swingtrade/api/service/ScanService.java</files>
  <action>
Two fixes needed in ScanService.java:

1. Replace non-existent `signalEngine.generateSignal(symbol)` with existing `signalEngine.generateSignalForSymbolNow(symbol)` (line 45):
   - SignalEngine only has `generateSignalForSymbolNow(String)` method
   - This method is void (returns nothing) but triggers signal generation
   - No return value check needed

2. Fix setter name if used - ScanResponse only has `setSymbolsScanned()` not `setSymbolsScannedCount()`:
   - Check if any code calls `setSymbolsScannedCount()` - replace with `setSymbolsScanned()`
   - If setting count, use `setSymbolsScanned(int)`
   - If setting list, use `setSymbolsScanned(List<String>)` - but now field renamed to `scannedSymbols` so use `setScannedSymbols()`
</action>
  <verify>
<automated>cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl api -q 2>&1 | grep -i "ScanService" || echo "No ScanService errors"</automated>
  </verify>
  <done>
ScanService.java compiles without errors. Uses `generateSignalForSymbolNow()` API, uses correct setter `setScannedSymbols()` or `setSymbolsScanned()`.
  </done>
</task>

</tasks>

<verification>
1. Verify ScanResponse compiles: `grep -n "symbolsScanned" api/src/main/java/com/swingtrade/api/dto/ScanResponse.java`
   - Should show Integer version and List<String> version with different names
2. Verify ScanService compiles: `grep -n "generateSignal" api/src/main/java/com/swingtrade/api/service/ScanService.java`
   - Should only show `generateSignalForSymbolNow`
3. Run compile: `cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl api -q`
4. Verify no duplicate field errors
</verification>

<success_criteria>
- ScanResponse.java compiles without "duplicate field" errors
- ScanService.java uses `generateSignalForSymbolNow()` instead of non-existent `generateSignal()`
- No calls to `setSymbolsScannedCount()` exist
- `mvn compile -pl api` succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-layer/05-05-SUMMARY.md`
</output>
