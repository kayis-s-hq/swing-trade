---
phase: 05-api-layer
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/main/java/com/swingtrade/api/service/PerformanceService.java
  - api/src/main/java/com/swingtrade/api/dto/PerformanceResponse.java
autonomous: true
requirements:
  - REQ-023
user_setup: []

# WORKTREE WORKFLOW ENFORCEMENT
# CRITICAL: This plan MUST be executed in a git worktree environment
# Root .planning/ is source of truth - worktree .planning/ is working copy
worktree_enforcement:
  required: true
  reason: "Prevents direct edits to root .planning/ on main branch"
  workflow:
    - step: 1
      action: "Verify worktree directory"
      command: "pwd | grep worktrees"
      fail_message: "ERROR: Must be in a worktree directory (e.g., .claude/worktrees/phase-05/)"
    - step: 2
      action: "Verify worktree branch"
      command: "git branch --show-current"
      expected_pattern: "worktree-phase-.*"
      fail_message: "ERROR: Must be on a worktree branch (e.g., worktree-phase-05)"
    - step: 3
      action: "Edit planning docs in worktree"
      path: ".planning/phases/05-api-layer/"
      note: "Do NOT edit .planning/ in root repository"
    - step: 4
      action: "Sync to root before merge"
      command: "Skill(\"superpowers:gsd-worktree-workflow --sync-to-root\")"
      when: "Before merging worktree branch to main"
    - step: 5
      action: "Verify before merge"
      command: "gsd:verify"
      when: "After plan completion, before merge"
user_setup: []
must_haves:
  truths:
    - GET /api/performance returns PortfolioResponse with P&L as BigDecimal
    - P&L calculations use BigDecimal consistently
  artifacts:
    - path: api/src/main/java/com/swingtrade/api/service/PerformanceService.java
      provides: "Performance stats calculation with BigDecimal P&L"
      min_lines: 150
    - path: api/src/main/java/com/swingtrade/api/dto/PerformanceResponse.java
      provides: "Response DTO with BigDecimal fields"
      min_lines: 50
  key_links:
    - from: api/src/main/java/com/swingtrade/api/service/PerformanceService.java
      to: broker/src/main/java/com/swingtrade/broker/engine/PaperTradingEngine.java
      via: "getTotalPnL()"
      pattern: "paperTradingEngine\\.getTotalPnL"
---

<objective>
Fix PerformanceService type mismatches for P&L calculations.

Purpose: Resolve blocker gaps where BigDecimal P&L is assigned to double variables, causing compile errors.

Output: PerformanceService.java that uses BigDecimal consistently for all P&L calculations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# PaperTradingEngine.getTotalPnL()
Returns BigDecimal - confirmed in broker/engine/PaperTradingEngine.java line 502.

# PerformanceService Issues
Gap 2, 3: PerformanceService has type mismatches:
- Line 34: `double totalPnL = calculateTotalPnL()` but `paperTradingEngine.getTotalPnL()` returns BigDecimal
- Line 64: `return paperTradingEngine.getTotalPnL()` returns BigDecimal assigned to double

# PerformanceResponse
Gap 3: Response DTO needs BigDecimal fields for P&L values.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PerformanceService P&L type from double to BigDecimal</name>
  <files>api/src/main/java/com/swingtrade/api/service/PerformanceService.java</files>
  <action>
Replace all `double` types with `BigDecimal` for P&L calculations:

1. Change method signature (line 32):
   - FROM: `public PerformanceStats getPerformanceStats()`
   - Keep same, but internal types change

2. Change local variables (lines 34-42):
   - FROM: `double totalPnL = calculateTotalPnL();`
   - TO: `BigDecimal totalPnL = paperTradingEngine.getTotalPnL();`

   - FROM: `double totalReturn = calculateTotalReturn(totalPnL);`
   - TO: `BigDecimal totalReturn = calculateTotalReturn(totalPnL);`

   - FROM: `double annualizedReturn = calculateAnnualizedReturn(totalReturn);`
   - TO: `BigDecimal annualizedReturn = calculateAnnualizedReturn(totalReturn);`

   - FROM: `double sharpeRatio = calculateSharpeRatio();`
   - TO: `BigDecimal sharpeRatio = calculateSharpeRatio();`

   - FROM: `double maxDrawdown = calculateMaxDrawdown();`
   - TO: `BigDecimal maxDrawdown = calculateMaxDrawdown();`

3. Update calculateTotalPnL() (lines 60-65):
   - FROM: `private double calculateTotalPnL()`
   - TO: `private BigDecimal calculateTotalPnL()`
   - FROM: `return paperTradingEngine.getTotalPnL();`
   - TO: `return paperTradingEngine.getTotalPnL();` (no change - already returns BigDecimal)

4. Update calculateTotalReturn() (lines 70-73):
   - FROM: `private double calculateTotalReturn(double totalPnL)`
   - TO: `private BigDecimal calculateTotalReturn(BigDecimal totalPnL)`
   - Update calculation: `return totalPnL.divide(initialCapital, 4, RoundingMode.HALF_UP).multiply(BigDecimal.valueOf(100));`

5. Update calculateAnnualizedReturn(), calculateSharpeRatio(), calculateMaxDrawdown() to return BigDecimal instead of double.

6. Update PerformanceStats constructor to accept BigDecimal for all percentage fields (already does).
</action>
  <verify>
<automated>cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl api -q 2>&1 | grep -i "PerformanceService" || echo "No PerformanceService errors"</automated>
  </verify>
  <done>
PerformanceService.java compiles without type mismatch errors. All P&L fields use BigDecimal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PerformanceResponse DTO to use BigDecimal</name>
  <files>api/src/main/java/com/swingtrade/api/dto/PerformanceResponse.java</files>
  <action>
Ensure PerformanceResponse DTO uses BigDecimal for all P&L related fields:

1. Check totalPnL field - should be BigDecimal
2. Check realizedPnL field - should be BigDecimal
3. Check unrealizedPnL field - should be BigDecimal
4. Check winRate field - should be BigDecimal (not double)

If any fields are double, change to BigDecimal and add appropriate setters.
</action>
  <verify>
<automated>cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl api -q 2>&1 | grep -i "PerformanceResponse" || echo "No PerformanceResponse errors"</automated>
  </verify>
  <done>
PerformanceResponse.java compiles with all P&L fields as BigDecimal.
  </done>
</task>

</tasks>

<verification>
1. Verify PerformanceService compiles: `cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl api -q 2>&1`
2. Verify no BigDecimal to double conversion errors
3. Check getPerformanceStats() returns correct types
</verification>

<success_criteria>
- PerformanceService compiles without type mismatch errors
- getTotalPnL() returns BigDecimal
- calculateTotalReturn() returns BigDecimal
- All P&L calculations use BigDecimal consistently
- PerformanceResponse has BigDecimal fields
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-layer/05-06-SUMMARY.md`
</output>
