---
phase: 05-api-layer
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - data/src/main/java/com/swingtrade/data/repository/PositionRepository.java
  - api/src/main/java/com/swingtrade/api/Position.java
autonomous: true
requirements:
  - REQ-016
user_setup: []

# WORKTREE WORKFLOW ENFORCEMENT
# CRITICAL: This plan MUST be executed in a git worktree environment
# Root .planning/ is source of truth - worktree .planning/ is working copy
worktree_enforcement:
  required: true
  reason: "Prevents direct edits to root .planning/ on main branch"
  workflow:
    - step: 1
      action: "Verify worktree directory"
      command: "pwd | grep worktrees"
      fail_message: "ERROR: Must be in a worktree directory (e.g., .claude/worktrees/phase-05/)"
    - step: 2
      action: "Verify worktree branch"
      command: "git branch --show-current"
      expected_pattern: "worktree-phase-.*"
      fail_message: "ERROR: Must be on a worktree branch (e.g., worktree-phase-05)"
    - step: 3
      action: "Edit planning docs in worktree"
      path: ".planning/phases/05-api-layer/"
      note: "Do NOT edit .planning/ in root repository"
    - step: 4
      action: "Sync to root before merge"
      command: "Skill(\"superpowers:gsd-worktree-workflow --sync-to-root\")"
      when: "Before merging worktree branch to main"
    - step: 5
      action: "Verify before merge"
      command: "gsd:verify"
      when: "After plan completion, before merge"
user_setup: []
must_haves:
  truths:
    - PositionRepository.findOpenBySymbol() returns Optional<PositionEntity>
    - Position domain DTO has all required fields (direction, entryPrice, stopLoss, target, quantity)
  artifacts:
    - path: data/src/main/java/com/swingtrade/data/repository/PositionRepository.java
      provides: "Position queries returning Optional for single result"
      min_lines: 83
    - path: api/src/main/java/com/swingtrade/api/Position.java
      provides: "Position domain DTO with all fields"
      min_lines: 150
  key_links:
    - from: api/src/main/java/com/swingtrade/api/PositionService.java
      to: data/src/main/java/com/swingtrade/data/repository/PositionRepository.java
      via: "findOpenBySymbol()"
      pattern: "positionRepository\\.findOpenBySymbol"
---

<objective>
Fix PositionRepository return type and Position DTO field mismatches.

Purpose: Resolve major gaps where PositionRepository.findOpenBySymbol() returns Optional but code expects List, and Position DTO is missing fields.

Output: PositionRepository with correct Optional return type, Position DTO with all required fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Gap 13: PositionRepository Return Type
Gap 13: PositionRepository.findOpenBySymbol() returns Optional<PositionEntity> but PositionService line 55 expects List<PositionEntity>.

Current PositionRepository.java line 32-33:
```java
@Query("SELECT p FROM PositionEntity p WHERE p.symbol = :symbol AND p.status = 'OPEN'")
Optional<PositionEntity> findOpenBySymbol(@Param("symbol") String symbol);
```

This is CORRECT for a single result. Need to update PositionService to handle Optional.

# Position Domain DTO
api/src/main/java/com/swingtrade/api/Position.java may be missing fields:
- direction (BUY/SELL)
- entryPrice
- stopLoss
- target
- quantity
- status
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify PositionRepository.findOpenBySymbol return type</name>
  <files>data/src/main/java/com/swingtrade/data/repository/PositionRepository.java</files>
  <action>
PositionRepository.findOpenBySymbol() already returns Optional<PositionEntity> correctly. This is the proper JPA pattern for single-result queries.

No changes needed to PositionRepository.java. The method signature is:
```java
@Query("SELECT p FROM PositionEntity p WHERE p.symbol = :symbol AND p.status = 'OPEN'")
Optional<PositionEntity> findOpenBySymbol(@Param("symbol") String symbol);
```

This is correct. PositionService should handle Optional<PositionEntity> properly (which we're fixing in plan 05-07).
</action>
  <verify>
<automated>cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl data -q 2>&1 | grep -i "PositionRepository" || echo "No PositionRepository errors"</automated>
  </verify>
  <done>
PositionRepository.java compiles correctly. findOpenBySymbol() returns Optional<PositionEntity> as expected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add missing fields to Position domain DTO</name>
  <files>api/src/main/java/com/swingtrade/api/Position.java</files>
  <action>
Ensure Position.java DTO has all required fields for position responses:

Required fields:
- symbol: String
- direction: String (BUY/SELL)
- entryPrice: BigDecimal or Double
- stopLoss: BigDecimal or Double (optional)
- entryDate: LocalDate or LocalDateTime
- target: BigDecimal or Double (optional)
- quantity: Integer
- status: String (OPEN/CLOSED/STOPPED/TARGET_HIT)
- currentPrice: BigDecimal or Double (optional)

Check current Position.java and add any missing fields with getters/setters.

If Position.java doesn't exist, create it as a DTO for API responses.
</action>
  <verify>
<automated>cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl api -q 2>&1 | grep -i "Position.java" || echo "No Position.java errors"</automated>
  </verify>
  <done>
Position.java DTO exists with all required fields. Compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. Check PositionRepository: `grep -A2 "findOpenBySymbol" data/src/main/java/com/swingtrade/data/repository/PositionRepository.java`
2. Check Position.java fields: `cat api/src/main/java/com/swingtrade/api/Position.java`
3. Compile data module: `cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl data -q`
4. Compile api module: `cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl api -q`
</verification>

<success_criteria>
- PositionRepository.findOpenBySymbol() returns Optional<PositionEntity>
- Position.java DTO has all required fields
- PositionService properly handles Optional return type
- All position-related endpoints compile
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-layer/05-11-SUMMARY.md`
</output>
