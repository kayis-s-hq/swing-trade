---
phase: 05-api-layer
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/main/java/com/swingtrade/api/config/ActuatorConfig.java
autonomous: true
requirements:
  - REQ-022
user_setup: []
must_haves:
  truths:
    - GET /api/health returns health status
    - ActuatorConfig.java compiles without deprecated import errors
  artifacts:
    - path: api/src/main/java/com/swingtrade/api/config/ActuatorConfig.java
      provides: "Actuator configuration without deprecated imports"
      min_lines: 0
  key_links:
    - from: api/src/main/java/com/swingtrade/api/config/ActuatorConfig.java
      to: Spring Boot Actuator
      via: "WebMvcEndpointHandlerMapping"
      pattern: "import.*actuate"
---

<objective>
Remove deprecated Spring Boot Actuator imports from ActuatorConfig.java.

Purpose: Resolve blocker where ActuatorConfig.java has imports that don't exist in Spring Boot 3.x.

Output: ActuatorConfig.java removed or refactored to use valid Spring Boot 3.x APIs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# ActuatorConfig Deprecated Imports
Gap 10: ActuatorConfig.java has imports that don't exist in Spring Boot 3.x:
- Line 3: `import io.micrometer.core.audit.AuditLogger;` - AuditLogger class removed
- Lines 6-14: Actuator endpoint web classes - many removed in Spring Boot 3.x

# Spring Boot 3.x Changes
In Spring Boot 3.x:
- WebMvcEndpointHandlerMapping is now internal
- Custom actuator endpoint configuration is not typically needed
- Audit logging is handled differently

# Recommendation
Remove ActuatorConfig.java entirely unless there's a specific need for custom actuator configuration. Spring Boot 3.x handles actuator endpoints automatically.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated ActuatorConfig.java</name>
  <files>api/src/main/java/com/swingtrade/api/config/ActuatorConfig.java</files>
  <action>
Remove ActuatorConfig.java entirely. This file contains deprecated Spring Boot Actuator imports that don't exist in Spring Boot 3.x:

1. Check if ActuatorConfig is used anywhere:
   - If only imported by other config files, it can be safely removed
   - Spring Boot 3.x provides actuator endpoints automatically

2. Delete the file:
   rm api/src/main/java/com/swingtrade/api/config/ActuatorConfig.java

3. If there are health check endpoints in HealthController that depend on this, verify they work without it.

Rationale: Custom WebMvcEndpointHandlerMapping configuration is no longer needed in Spring Boot 3.x. The actuator endpoints are configured automatically.
</action>
  <verify>
<automated>cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl api -q 2>&1 | grep -i "ActuatorConfig" || echo "ActuatorConfig removed or no errors"</automated>
  </verify>
  <done>
ActuatorConfig.java is removed. Project compiles without deprecated actuator import errors. Health endpoint still works.
  </done>
</task>

</tasks>

<verification>
1. Check if ActuatorConfig is referenced: `grep -r "ActuatorConfig" api/`
2. Remove file if not needed
3. Compile to verify no errors
</verification>

<success_criteria>
- ActuatorConfig.java removed or updated to use Spring Boot 3.x APIs
- No deprecated import errors
- Health endpoint still functional
- `mvn compile -pl api` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-layer/05-08-SUMMARY.md`
</output>
