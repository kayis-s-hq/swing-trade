---
phase: 05-api-layer
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - data/src/main/java/com/swingtrade/data/repository/SignalRepository.java
autonomous: true
requirements:
  - REQ-021
user_setup: []

# WORKTREE WORKFLOW ENFORCEMENT
# CRITICAL: This plan MUST be executed in a git worktree environment
# Root .planning/ is source of truth - worktree .planning/ is working copy
worktree_enforcement:
  required: true
  reason: "Prevents direct edits to root .planning/ on main branch"
  workflow:
    - step: 1
      action: "Verify worktree directory"
      command: "pwd | grep worktrees"
      fail_message: "ERROR: Must be in a worktree directory (e.g., .claude/worktrees/phase-05/)"
    - step: 2
      action: "Verify worktree branch"
      command: "git branch --show-current"
      expected_pattern: "worktree-phase-.*"
      fail_message: "ERROR: Must be on a worktree branch (e.g., worktree-phase-05)"
    - step: 3
      action: "Edit planning docs in worktree"
      path: ".planning/phases/05-api-layer/"
      note: "Do NOT edit .planning/ in root repository"
    - step: 4
      action: "Sync to root before merge"
      command: "Skill(\"superpowers:gsd-worktree-workflow --sync-to-root\")"
      when: "Before merging worktree branch to main"
    - step: 5
      action: "Verify before merge"
      command: "gsd:verify"
      when: "After plan completion, before merge"
user_setup: []
must_haves:
  truths:
    - GET /api/signals?filters returns filtered signals
    - SignalRepository has query methods for date range and signal type filtering
  artifacts:
    - path: data/src/main/java/com/swingtrade/data/repository/SignalRepository.java
      provides: "Signal queries with date range and signal type filters"
      min_lines: 113
  key_links:
    - from: data/src/main/java/com/swingtrade/data/repository/SignalRepository.java
      to: api/src/main/java/com/swingtrade/api/service/SignalService.java
      via: "findByDateRangeAndSignalType()"
      pattern: "signalRepository\\.findByDateRangeAndSignalType"
---

<objective>
Add missing query methods to SignalRepository for signal filtering.

Purpose: Resolve major gap where SignalService calls repository methods that don't exist (findByDateRangeAndSignalType, findBuySignalsSince).

Output: SignalRepository with complete set of query methods for filtering signals by date range and type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Gap 12: SignalRepository Missing Query Methods
Gap 12: SignalRepository is missing two query methods:
- Line 85: `findByDateRangeAndSignalType()` - used by SignalService.getSignalsByDateRange()
- Line 110: `findBuySignalsSince()` - used by SignalService.getSignalsByType()

# SignalRepository Current State
The repository already has similar methods but with different signatures:
- `findBuySignalsSince(LocalDate, Pageable)` EXISTS (lines 52-56)
- `findByDateRangeAndSignalType(LocalDate, LocalDate, String, Pageable)` EXISTS (lines 67-73)

# Action Required
The methods exist in the code but were marked as missing. Need to verify:
1. Method signatures match what SignalService expects
2. Query annotations are correct
3. Pageable parameter is handled consistently
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and fix SignalRepository query methods</name>
  <files>data/src/main/java/com/swingtrade/data/repository/SignalRepository.java</files>
  <action>
Check and fix SignalRepository query methods:

1. Verify findBuySignalsSince() exists (lines 52-56):
   - Signature: `List<SignalEntity> findBuySignalsSince(LocalDate date, Pageable pageable)`
   - Query: `@Query("SELECT s FROM SignalEntity s WHERE s.signalType = 'BUY' AND s.date >= :date ORDER BY s.date DESC, s.createdAt DESC")`
   - This looks correct, ensure it's being used properly

2. Verify findByDateRangeAndSignalType() exists (lines 67-73):
   - Signature: `List<SignalEntity> findByDateRangeAndSignalType(LocalDate startDate, LocalDate endDate, String signalType, Pageable pageable)`
   - Query: `@Query("SELECT s FROM SignalEntity s WHERE s.date BETWEEN :startDate AND :endDate AND s.signalType = :signalType ORDER BY s.date DESC")`
   - This looks correct, ensure it's being used properly

3. If methods don't compile, check:
   - JPA query syntax (BETWEEN for dates is valid)
   - Parameter annotations (@Param) are correct
   - Return type matches (List<SignalEntity>)

If methods exist but SignalService has issues, update SignalService to match repository signatures.
</action>
  <verify>
<automated>cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl data -q 2>&1 | grep -i "SignalRepository" || echo "No SignalRepository errors"</automated>
  </verify>
  <done>
SignalRepository.compiles with all required query methods. SignalService can call findByDateRangeAndSignalType() and findBuySignalsSince() without errors.
  </done>
</task>

</tasks>

<verification>
1. Check SignalRepository compilation: `cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl data -q 2>&1`
2. Check SignalService compilation: `cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl api -q 2>&1`
3. Verify method signatures match between repository and service
</verification>

<success_criteria>
- SignalRepository.findBuySignalsSince() compiles
- SignalRepository.findByDateRangeAndSignalType() compiles
- SignalService.getSignalsByDateRange() compiles
- SignalService.getSignalsByType() compiles
- All signal filtering endpoints functional
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-layer/05-10-SUMMARY.md`
</output>
