---
phase: 05-api-layer
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - broker/pom.xml
  - llm/src/test/java/com/swingtrade/llm/test/LlmWireMockRule.java
  - data/src/test/java/com/swingtrade/data/test/UpstoxWireMockRule.java
autonomous: true
requirements: [REQ-020, REQ-021, REQ-022]
user_setup: []
must_haves:
  truths:
    - "Project builds successfully with mvn clean install"
    - "All dependencies are available from Maven Central"
    - "WireMock test helper methods use current API (not deprecated)"
    - "No compilation errors in llm or data test modules"
  artifacts:
    - path: "pom.xml"
      provides: "Parent POM with validated testcontainers version"
    - path: "broker/pom.xml"
      provides: "Broker module POM with optional dependencies properly scoped"
    - path: "llm/src/test/java/com/swingtrade/llm/test/LlmWireMockRule.java"
      provides: "LlmWireMockRule using WireMock 3.8.0 compatible API"
    - path: "data/src/test/java/com/swingtrade/data/test/UpstoxWireMockRule.java"
      provides: "UpstoxWireMockRule using WireMock 3.8.0 compatible API"
  key_links:
    - from: "pom.xml"
      to: "broker/pom.xml"
      via: "Parent POM version management"
      pattern: "parent version inherits testcontainers versions"
    - from: "llm/src/test/java/com/swingtrade/llm/test/LlmWireMockRule.java"
      to: "wiremock library"
      via: "wireMockServer.findAll() with WireMock matching API"
      pattern: "wireMockServer.findAll\\(.*Requested.*\\)"

---

<objective>
Fix build failures blocking Phase 05 API Layer testing and wiring validation.

Purpose: The project cannot compile due to missing/unavailable dependencies and deprecated WireMock method calls. These build failures prevent verification of API wiring implemented in plan 05-02.

Output: Clean Maven build with all dependencies resolved, deprecated WireMock methods replaced with current API, project compiles to JAR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-layer/05-VERIFICATION.md
@.planning/phases/05-api-layer/05-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix testcontainers version in parent POM</name>
  <files>pom.xml</files>
  <action>
Update pom.xml to fix testcontainers version compatibility:

1. Locate the properties section in pom.xml (around line 26-57)
2. Add or update testcontainers version property to a valid, available version:
   - Current problematic version: 1.19.3 (pinned too specifically, not available)
   - Replace with: 1.19.7 or latest stable (check Maven Central for available versions)
   - Add this property after other dependency version properties (after slf4j, logback):
     `<testcontainers.version>1.19.7</testcontainers.version>`

3. In dependencyManagement section (around line 59-250), add testcontainers BOM import:
   ```xml
   <!-- TestContainers -->
   <dependency>
     <groupId>org.testcontainers</groupId>
     <artifactId>testcontainers-bom</artifactId>
     <version>${testcontainers.version}</version>
     <type>pom</type>
     <scope>import</scope>
   </dependency>
   ```
   Place this after the Spring Boot dependencies block, before TA4J.

4. Verify wiremock version is correctly set (line 56: should be 3.8.0 or later)

5. Save and validate syntax (no duplicate tags, well-formed XML)

Implementation note: testcontainers is NOT in parent pom dependencyManagement by default. We're adding BOM import to make versions available to child modules without explicit version declarations.
  </action>
  <verify>
    <automated>grep -n "testcontainers.version" /Users/kayisrahman/Documents/workspace/ideas/swing-trade/pom.xml</automated>
  </verify>
  <done>pom.xml has valid testcontainers.version property (1.19.7 or later), BOM import added to dependencyManagement</done>
</task>

<task type="auto">
  <name>Task 2: Update broker/pom.xml - Comment out or remove unavailable optional dependencies</name>
  <files>broker/pom.xml</files>
  <action>
Update broker/pom.xml to remove/comment build failures from unavailable dependencies:

1. Verify lines 78-96 (Telegram Bot API):
   - Already commented out with `<!-- -->` wrapper
   - Marked with `<optional>true</optional>`
   - This is correct, leave as-is

2. Verify lines 88-96 (Zerodha Kite Connect):
   - Already commented out with `<!-- -->` wrapper
   - Marked with `<optional>true</optional>`
   - This is correct, leave as-is

3. Check lines 167-176 (Testcontainers Redis - NOT available in Maven Central):
   - Currently commented out (good)
   - Marked with `<optional>true</optional>`
   - Verify comment wraps the entire dependency block
   - This is correct, leave as-is

4. Ensure testcontainers core and postgresql dependencies remain uncommented (lines 158-181):
   - `org.testcontainers:testcontainers` - KEEP UNCOMMENTED (no version needed, inherited from parent BOM)
   - `org.testcontainers:junit-jupiter` - KEEP UNCOMMENTED
   - `org.testcontainers:postgresql` - KEEP UNCOMMENTED
   - These use versions from parent pom's testcontainers BOM

5. After updating parent pom, the testcontainers dependencies in broker should resolve automatically (remove any explicit version attributes if present)

6. Verify no duplicate dependency declarations for testcontainers

Implementation note: optional=true prevents build failure if libraries are unavailable. Commented blocks tell developers these are deferred (Telegram, Kite Connect, Redis testcontainer).
  </action>
  <verify>
    <automated>grep -A5 "testcontainers" /Users/kayisrahman/Documents/workspace/ideas/swing-trade/broker/pom.xml | grep -v "<!--"</automated>
  </verify>
  <done>broker/pom.xml has only active testcontainers dependencies (core, junit-jupiter, postgresql), optional/unavailable ones remain commented</done>
</task>

<task type="auto">
  <name>Task 3: Fix LlmWireMockRule - Replace deprecated countByUri() with findAll()</name>
  <files>llm/src/test/java/com/swingtrade/llm/test/LlmWireMockRule.java</files>
  <action>
Update LlmWireMockRule to use current WireMock 3.8.0 API:

1. Replace deprecated method call on line 105:
   OLD: `return wireMockServer.countByUri(endpoint);`
   NEW: `return wireMockServer.findAll(WireMock.anyRequestedFor(WireMock.urlPathMatching(".*" + endpoint + ".*"))).size();`

   Why: countByUri() was removed in WireMock 3.x. The replacement uses findAll() with a matcher pattern, same behavior.

2. Ensure imports are correct at top of file:
   - `import com.github.tomakehurst.wiremock.client.WireMock;`
   - `import com.github.tomakehurst.wiremock.matching.UrlPathPattern;`
   These should already exist (used in other methods like getAllChatCompletionsRequests on line 252)

3. No other methods need changes (verifyChatCompletionsCalled, getAllChatCompletionsRequests, resetAll are all using correct API)

4. Optional: Add javadoc clarification:
   Change javadoc on line 99-102 to note this counts ALL requests matching the endpoint path pattern (not just specific endpoint).

5. Verify method signature stays the same:
   `public int getInteractionCount(String endpoint)` - no changes to signature

Implementation note: This is a straightforward migration from deprecated to modern WireMock API. The findAll() method returns a list, .size() gives the count.
  </action>
  <verify>
    <automated>grep -n "getInteractionCount" /Users/kayisrahman/Documents/workspace/ideas/swing-trade/llm/src/test/java/com/swingtrade/llm/test/LlmWireMockRule.java</automated>
  </verify>
  <done>LlmWireMockRule.getInteractionCount() uses findAll() instead of deprecated countByUri()</done>
</task>

<task type="auto">
  <name>Task 4: Fix UpstoxWireMockRule - Replace deprecated methods with findAll()</name>
  <files>data/src/test/java/com/swingtrade/data/test/UpstoxWireMockRule.java</files>
  <action>
Update UpstoxWireMockRule to use current WireMock 3.8.0 API (same pattern as LlmWireMockRule):

1. First, read the file to identify deprecated method calls:
   - Look for `.count()`, `.getAllRequests()`, or `.countByUri()` patterns
   - These are likely in similar methods to LlmWireMockRule

2. Replace any deprecated calls:
   - OLD: `wireMockServer.countByUri(endpoint)`
   - NEW: `wireMockServer.findAll(WireMock.anyRequestedFor(WireMock.urlPathMatching(".*" + endpoint + ".*"))).size()`

   - OLD: `wireMockServer.getAllRequests()`
   - NEW: `wireMockServer.findAll(WireMock.anyRequestedFor(WireMock.anything()))`

   - OLD: `wireMockServer.count()` (direct count of all requests)
   - NEW: `wireMockServer.findAll(WireMock.anyRequestedFor(WireMock.anything())).size()`

3. Ensure imports match:
   - `import com.github.tomakehurst.wiremock.client.WireMock;`

4. Verify no other deprecated WireMock methods are used in this file

5. Keep method signatures unchanged - only internal implementations change

Implementation note: UpstoxWireMockRule follows same pattern as LlmWireMockRule. Identify deprecated calls and replace with findAll() + matcher + size().
  </action>
  <verify>
    <automated>grep -n "\.count()\|\.getAllRequests()\|\.countByUri(" /Users/kayisrahman/Documents/workspace/ideas/swing-trade/data/src/test/java/com/swingtrade/data/test/UpstoxWireMockRule.java</automated>
  </verify>
  <done>UpstoxWireMockRule uses current WireMock API, no deprecated method calls remain</done>
</task>

<task type="auto">
  <name>Task 5: Verify clean Maven build - mvn clean install succeeds</name>
  <files>pom.xml</files>
  <action>
Verify the project builds cleanly after dependency and test code fixes:

1. Run Maven build for entire project:
   ```bash
   cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade
   mvn clean install -DskipTests=true
   ```
   (Skip tests first to isolate build issues from test failures)

2. Expected result: BUILD SUCCESS with all modules compiled
   - core module compiles
   - data module compiles
   - strategy module compiles
   - llm module compiles (test classes compile without errors)
   - broker module compiles
   - api module compiles

3. If build fails, check error messages:
   - If dependency not found: verify pom.xml changes are correct
   - If compilation error in test code: verify WireMock method fixes are applied
   - If version conflict: check for duplicate version declarations

4. Once `mvn clean install` succeeds, run with tests:
   ```bash
   mvn clean install
   ```

5. Test failures are expected at this point (gaps in plan 05-02 not yet executed), but compilation must succeed

6. Check that no compilation errors appear in:
   - llm test module (WireMock fixes should allow compilation)
   - data test module (WireMock fixes should allow compilation)
   - broker test module (testcontainers dependencies should resolve)

Implementation note: Skip tests on first run to isolate build/compilation issues from test logic issues. Once build succeeds, full test run is safe.
  </action>
  <verify>
    <automated>cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn clean install -DskipTests=true 2>&1 | tail -20</automated>
  </verify>
  <done>mvn clean install -DskipTests=true succeeds with BUILD SUCCESS, all modules compile without errors</done>
</task>

</tasks>

<verification>
After all tasks complete, verify the gap closure:

1. **Dependency Resolution:**
   - grep pom.xml for testcontainers.version property (should exist, value like 1.19.7)
   - broker/pom.xml testcontainers dependencies have no explicit version (inherited from parent BOM)
   - Optional dependencies (Telegram, Kite, Redis) remain commented

2. **WireMock API Migration:**
   - LlmWireMockRule.getInteractionCount() line 105 uses findAll(), not countByUri()
   - UpstoxWireMockRule uses findAll() instead of deprecated methods
   - No .count(), .getAllRequests(), or .countByUri() calls remain in test code

3. **Compilation:**
   - mvn clean compile succeeds (no javac errors)
   - No "method not found" errors in test modules
   - No "dependency not found" errors in broker/pom.xml

4. **Build Success:**
   - mvn clean install -DskipTests=true produces BUILD SUCCESS
   - api-1.0.0.jar exists in api/target/
   - All module JARs exist in respective target/ directories

5. **No Regressions:**
   - No existing working code modified (only build config and deprecated test methods)
   - Non-test code remains unchanged (avoids side effects)
</verification>

<success_criteria>
- [ ] Parent pom.xml has testcontainers.version property (1.19.7+)
- [ ] Parent pom.xml has testcontainers BOM import in dependencyManagement
- [ ] broker/pom.xml testcontainers dependencies inherit version from parent
- [ ] Telegram, Kite, Redis optional dependencies remain commented out
- [ ] LlmWireMockRule.getInteractionCount() uses findAll() API
- [ ] UpstoxWireMockRule uses findAll() instead of deprecated methods
- [ ] mvn clean compile succeeds with no javac errors
- [ ] mvn clean install -DskipTests=true produces BUILD SUCCESS
- [ ] api/target/api-1.0.0.jar exists and is valid JAR file
- [ ] All 6 modules compile successfully

Goal achieved: Build fixes allow project to compile cleanly. API wiring validation (plan 05-02) can now proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-layer/05-03-SUMMARY.md` documenting:
- Dependency versions fixed
- WireMock API migration completed
- Build verification results
- All modules compile cleanly
</output>
