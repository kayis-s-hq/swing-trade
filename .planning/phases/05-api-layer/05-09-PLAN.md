---
phase: 05-api-layer
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/main/java/com/swingtrade/api/controller/TradeLabelController.java
autonomous: true
requirements:
  - REQ-022
user_setup: []
must_haves:
  truths:
    - API layer compiles without missing class errors
    - TradeLabelController either removed or has working TradeLabel entities
  artifacts:
    - path: api/src/main/java/com/swingtrade/api/controller/TradeLabelController.java
      provides: "Trade labeling endpoint"
      min_lines: 163
  key_links:
    - from: api/src/main/java/com/swingtrade/api/controller/TradeLabelController.java
      to: data/src/main/java/com/swingtrade/data/entity/TradeLabel.java
      via: "TradeLabel entity"
      pattern: "TradeLabel"
---

<objective>
Fix TradeLabelController missing class errors.

Purpose: Resolve blocker where TradeLabelController references non-existent TradeLabel and TradeLabelService classes.

Output: TradeLabelController either removed (if not needed) or complete TradeLabel implementation added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# TradeLabelController Missing Classes
Gap 11: TradeLabelController.java references non-existent classes:
- Line 3: `import com.swingtrade.data.entity.TradeLabel;` - entity doesn't exist
- Line 4: `import com.swingtrade.data.service.TradeLabelService;` - service doesn't exist
- Lines 21, 23, 119: Uses TradeLabel and TradeLabelService throughout

# TradeLabel Feature Scope
Trade labeling is part of Phase 7 (Observability) according to ROADMAP.md:
- REQ-036: Trade labelling is in Phase 7, not Phase 5

# Recommendation
Since TradeLabel is a Phase 7 feature, remove TradeLabelController.java from Phase 5. It's not required for basic API functionality (signals, positions, trades, performance, scan).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove TradeLabelController (Phase 7 feature)</name>
  <files>api/src/main/java/com/swingtrade/api/controller/TradeLabelController.java</files>
  <action>
Remove TradeLabelController.java since TradeLabel feature is in Phase 7 (Observability), not Phase 5 (API Layer).

The controller is not needed for Phase 5 requirements:
- REQ-016: Paper trading engine
- REQ-017: Risk controls
- REQ-020 to REQ-024: API endpoints (signals, positions, performance, scan, health)

Trade labeling (REQ-036) is explicitly in Phase 7 - removing this controller keeps Phase 5 scope focused on core trading functionality.

Action: Delete api/src/main/java/com/swingtrade/api/controller/TradeLabelController.java
</action>
  <verify>
<automated>cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl api -q 2>&1 | grep -i "TradeLabel" || echo "TradeLabel removed or no errors"</automated>
  </verify>
  <done>
TradeLabelController.java removed. Project compiles without TradeLabel missing class errors. Phase 5 API layer complete without Phase 7 features.
  </done>
</task>

</tasks>

<verification>
1. Remove TradeLabelController.java
2. Compile: `cd /Users/kayisrahman/Documents/workspace/ideas/swing-trade && mvn compile -pl api -q`
3. Verify no TradeLabel errors
</verification>

<success_criteria>
- TradeLabelController.java removed
- No TradeLabel or TradeLabelService errors
- Core API endpoints (signals, positions, trades, performance, scan) still functional
- `mvn compile -pl api` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-layer/05-09-SUMMARY.md`
</output>
