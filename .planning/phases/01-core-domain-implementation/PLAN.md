# Phase 1: Core Domain Implementation

**Phase ID:** 01
**Status:** ✅ Complete
**Date:** 2026-03-08

---

## Objective

Complete domain models with all required fields and validation for the swing trading system.

---

## Deliverables

| ID | Model | Status | File |
|----|-------|--------|------|
| 1.1 | Stock | ✅ Complete | `core/src/main/java/com/swingtrade/domain/Stock.java` |
| 1.2 | OhlcvCandle | ✅ Complete | `core/src/main/java/com/swingtrade/domain/OhlcvCandle.java` |
| 1.3 | Signal | ✅ Complete | `core/src/main/java/com/swingtrade/domain/Signal.java` |
| 1.4 | Position | ✅ Complete | `core/src/main/java/com/swingtrade/domain/Position.java` |
| 1.5 | Trade | ✅ Complete | `core/src/main/java/com/swingtrade/domain/Trade.java` |
| 1.6 | SentimentResult | ✅ Complete | `core/src/main/java/com/swingtrade/domain/SentimentResult.java` |

---

## Implementation Details

### 1.1 Stock Model

**Purpose:** Represents a stock entity with identifying and descriptive information.

**Fields:**
- `symbol` - Unique identifier (e.g., "RELIANCE", "TCS")
- `exchange` - NSE or BSE
- `name` - Full company name
- `sector` - Industry sector classification
- `isin` - International Securities Identification Number
- `lotSize` - Trading lot size
- `addedOn` - Date added to exchange universe

**Enums:**
- `Exchange` - NSE, BSE with full names
- `Sector` - 17 sectors (AUTO, BANK, CHEMICAL, etc.)

---

### 1.2 OhlcvCandle Model

**Purpose:** Represents candlestick data point for technical analysis.

**Fields:**
- `symbol` - Stock symbol
- `date` - Trading date
- `open` - Opening price
- `high` - Highest price
- `low` - Lowest price
- `close` - Closing price
- `volume` - Trading volume
- `adjClose` - Adjusted closing price

**Methods:**
- `getRange()` - Daily price range (high - low)
- `getChangePercent()` - Daily change percentage
- `isBullish()` - Close > Open
- `isBearish()` - Close < Open
- `of()` - Factory method with auto-calculated adjClose

---

### 1.3 Signal Model

**Purpose:** Represents trading signal generated by strategy engine.

**Fields:**
- `id` - Database auto-generated ID
- `symbol` - Stock symbol
- `date` - Signal date
- `type` - BUY, SELL, or HOLD
- `confidence` - 0.0 to 1.0 (normalized)
- `reasoning` - Textual explanation
- `entryPrice` - Recommended entry price zone
- `stopLoss` - Stop loss price level
- `target` - Target price for profit taking
- `riskReward` - Calculated risk-reward ratio
- `indicators` - Technical indicators that triggered signal
- `generatedAt` - Timestamp when generated

**Enums:**
- `SignalType` - BUY, SELL, HOLD with descriptions

**Methods:**
- `create()` - Factory method with normalized confidence
- `isBuySignal()`, `isSellSignal()`, `isHoldSignal()` - Type checks

---

### 1.4 Position Model

**Purpose:** Represents an open position in a stock.

**Fields:**
- `id` - Database auto-generated ID
- `symbol` - Stock symbol
- `entryPrice` - Entry price
- `entryDate` - Entry date
- `quantity` - Number of shares
- `stopLoss` - Stop-loss price
- `target` - Target price
- `status` - OPEN, CLOSED, STOPPED, TARGET_HIT
- `entryReason` - Reason for entry
- `currentPrice` - Current market price

**Enums:**
- `PositionStatus` - OPEN, CLOSED, STOPPED, TARGET_HIT

**Methods:**
- `createWithRisk()` - Factory with calculated stop loss (entry - 2*ATR) and target (entry + 2.5*risk)
- `calculateUnrealizedPnL()` - Position value - cost basis
- `calculatePnLPercent()` - Percentage P&L
- `isOpen()`, `isClosed()` - Status checks

---

### 1.5 Trade Model

**Purpose:** Represents complete trade lifecycle from entry to exit.

**Fields:**
- `id` - Database auto-generated ID
- `positionId` - Associated position ID
- `symbol` - Stock symbol
- `entryDate` - Entry date
- `exitDate` - Exit date (null if open)
- `entryPrice` - Entry price
- `exitPrice` - Exit price (null if open)
- `quantity` - Number of shares
- `totalPnL` - Total profit/loss
- `durationDays` - Trade duration
- `tradeStatus` - OPEN, CLOSED, STOPPED, TARGET_HIT, TIME_STOP
- `entryReason` - Entry reason
- `exitReason` - Exit reason (null if open)
- `fees` - Total fees paid

**Enums:**
- `TradeStatus` - OPEN, CLOSED, STOPPED, TARGET_HIT, TIME_STOP

**Methods:**
- `open()` - Factory for new open trade
- `close()` - Close trade with exit details, calculates PnL and duration
- `isProfitable()` - Check if PnL > 0
- `isLoss()` - Check if PnL < 0
- `isOpen()` - Check if trade is open

---

### 1.6 SentimentResult Model

**Purpose:** Represents sentiment analysis result for a stock.

**Fields:**
- `id` - Database auto-generated ID
- `symbol` - Stock symbol
- `date` - Analysis date
- `score` - POSITIVE, NEUTRAL, NEGATIVE
- `summary` - Textual summary
- `rawContent` - Raw content analyzed
- `confidence` - 0.0 to 1.0 (normalized)
- `analyzedAt` - Timestamp

**Enums:**
- `SentimentScore` - POSITIVE, NEUTRAL, NEGATIVE with display names

**Methods:**
- `create()` - Factory method with normalized confidence
- `isPositive()`, `isNeutral()`, `isNegative()` - Score checks
- `supportsEntry()` - POSITIVE or NEUTRAL sentiment

---

## Completion Criteria

- [x] All 6 domain models implemented
- [x] Factory methods present (create, open, close, of)
- [x] Business methods implemented (P&L calculations, validations)
- [x] Enum types properly defined with metadata
- [x] Records used for immutability
- [x] Documentation comments present

---

## Next Steps

Proceed to **Phase 2: Strategy Engine** for technical analysis and signal generation implementation.
