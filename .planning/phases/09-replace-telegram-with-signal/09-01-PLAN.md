---
phase: 09-replace-telegram-with-signal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - broker/src/main/java/com/swingtrade/broker/telegram/SignalNotificationService.java
  - broker/src/main/java/com/swingtrade/broker/telegram/SignalMessageFormatter.java
  - broker/src/main/java/com/swingtrade/broker/telegram/SignalConfig.java
  - broker/src/main/resources/application.properties
autonomous: true
requirements:
  - REQ-018
user_setup:
  - service: signal/signl4
    why: "Notification service replacing Telegram"
    env_vars:
      - name: SIGNAL_API_URL
        source: "Signal/Signl4 webhook configuration"
      - name: SIGNAL_API_TOKEN
        source: "Signal/Signl4 API settings"
    dashboard_config:
      - task: "Configure webhook endpoint"
        location: "Signal/Signl4 dashboard"
      - task: "Generate API token"
        location: "Signal/Signl4 API settings"

must_haves:
  truths:
    - "Signal receives all notification types that Telegram received"
    - "Signal configuration replaces all telegram.* configuration"
    - "All notification methods implemented (signals, positions, trades, commands, summaries, warnings, alerts)"
  artifacts:
    - path: "broker/src/main/java/com/swingtrade/broker/telegram/SignalNotificationService.java"
      provides: "Signal notification service with full feature parity"
      min_lines: 500
    - path: "broker/src/main/java/com/swingtrade/broker/telegram/SignalMessageFormatter.java"
      provides: "Message formatting for all notification types"
      min_lines: 400
    - path: "broker/src/main/java/com/swingtrade/broker/telegram/SignalConfig.java"
      provides: "Signal configuration class"
      contains: "ConfigurationProperties(prefix = signal)"
    - path: "broker/src/main/resources/application.properties"
      provides: "Signal configuration properties"
      contains: "signal.* properties"
  key_links:
    - from: "broker/src/main/java/com/swingtrade/broker/telegram/SignalNotificationService.java"
      to: "broker/src/main/java/com/swingtrade/broker/telegram/SignalMessageFormatter.java"
      via: "inject SignalMessageFormatter"
      pattern: "private final SignalMessageFormatter"
    - from: "broker/src/main/resources/application.properties"
      to: "broker/src/main/java/com/swingtrade/broker/telegram/SignalConfig.java"
      via: "@ConfigurationProperties(prefix = signal)"
      pattern: "@ConfigurationProperties.*signal"

---

<objective>
Expand Signal/Signl4 notification service to full feature parity with Telegram - implement all notification types including signals, positions, commands, daily summaries, risk warnings, and error alerts. Create SignalConfig and update application.properties with signal.* configuration properties.
</objective>

<execution_context>
/Users/kayisrahman/.claude/get-shit-done/workflows/execute-plan.md
/Users/kayisrahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
/Users/kayisrahman/Documents/workspace/ideas/swing-trade/.planning/REQUIREMENTS.md
/Users/kayisrahman/Documents/workspace/ideas/swing-trade/.planning/phases/09-replace-telegram-with-signal/09-CONTEXT.md

Existing implementations:
- SignalNotificationService.java - Current trade-only implementation
- SignalMessageFormatter.java - Current trade-only formatting
- TelegramNotificationService.java - Full feature reference
- TelegramMessageFormatter.java - Full formatting reference
- TelegramConfig.java - Configuration reference
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand SignalNotificationService with all notification methods</name>
  <files>broker/src/main/java/com/swingtrade/broker/telegram/SignalNotificationService.java</files>
  <behavior>
    - Add methods for: sendBuySignal, sendSellSignal, sendHoldSignal, sendPositionEntry, sendPositionUpdate, sendDailySummary, sendErrorAlert, sendPositionSizeAlert, sendRiskWarning, sendSystemStatus, sendMarketStatus
    - Add command handling for: /stop, /resume, /status, /help
    - Reuse pattern from TelegramNotificationService
    - Use SignalMessageFormatter for formatting
  </behavior>
  <action>
    Expand SignalNotificationService.java to implement all notification methods:

    1. Signal notifications:
       - sendBuySignal(Signal signal): Uses formatBuySignal
       - sendSellSignal(Signal signal): Uses formatSellSignal
       - sendHoldSignal(Signal signal): Uses formatHoldSignal

    2. Position notifications:
       - sendPositionEntry(Position position): Uses formatPositionEntry
       - sendPositionUpdate(Position position): Uses formatPositionUpdate
       - sendPositionExit(Position position, Trade trade): Uses formatPositionExit

    3. Trade notifications (keep existing):
       - sendTradeOpen(Position, Signal): Uses formatTradeOpen
       - sendTradeClose(Position, Trade): Uses formatTradeClose
       - sendStopLossHit(Position, exitPrice, pnl): Uses formatStopLossHit
       - sendTargetHit(Position, exitPrice, pnl): Uses formatTargetHit

    4. System notifications:
       - sendDailySummary(DailySummary summary): Uses formatDailySummary
       - sendErrorAlert(String, String, ErrorSeverity): Uses formatErrorAlert
       - sendPositionSizeAlert(String, currentSize, maxSize, percentage): Uses formatPositionSizeAlert
       - sendRiskWarning(String, riskPercent): Uses formatRiskWarning
       - sendSystemStatus(String status): Uses formatSystemStatus
       - sendMarketStatus(boolean isOpening): Uses formatMarketStatus

    5. Command handlers:
       - initializeCommandHandlers(): Sets up /stop, /resume, /status, /help
       - processCommand(String, String, String): Routes commands to handlers
       - handleStopCommand(String): Activates kill switch
       - handleResumeCommand(String): Deactivates kill switch
       - handleStatusCommand(String): Shows system status
       - handleHelpCommand(String): Shows available commands

    Reference TelegramNotificationService.java for implementation patterns.
    Keep existing quiet hours support and message truncation logic.
  </action>
  <verify>
    <automated>cd broker && mvn compile -q</automated>
  </verify>
  <done>
    SignalNotificationService has all notification methods:
    - sendBuySignal, sendSellSignal, sendHoldSignal
    - sendPositionEntry, sendPositionUpdate, sendPositionExit
    - sendTradeOpen, sendTradeClose, sendStopLossHit, sendTargetHit
    - sendDailySummary, sendErrorAlert, sendPositionSizeAlert, sendRiskWarning
    - sendSystemStatus, sendMarketStatus
    - Command handlers: /stop, /resume, /status, /help
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand SignalMessageFormatter with all formatting methods</name>
  <files>broker/src/main/java/com/swingtrade/broker/telegram/SignalMessageFormatter.java</files>
  <behavior>
    - Add format methods for: buy signal, sell signal, hold signal, position entry, position update, position exit, daily summary, error alert, position size alert, risk warning, system status, market status
    - Each method returns formatted string with emojis and markdown
    - Reuse helper methods from TelegramMessageFormatter
  </behavior>
  <action>
    Expand SignalMessageFormatter.java to implement all formatting methods:

    1. Signal formatting:
       - formatBuySignal(Signal signal): Adds BUY signal details with confidence, reasoning
       - formatSellSignal(Signal signal): Adds SELL signal details
       - formatHoldSignal(Signal signal): Adds HOLD signal with reasoning

    2. Position formatting:
       - formatPositionEntry(Position position): Entry details, quantity, price, SL/target
       - formatPositionUpdate(Position position): Current price, P&L, distance to SL/target
       - formatPositionExit(Position, Trade): Exit details, P&L, duration, exit reason

    3. Trade formatting (keep existing):
       - formatTradeOpen(Position, Signal): Trade open details
       - formatTradeClose(Position, Trade): Trade close details
       - formatStopLossHit(Position, exitPrice, pnl): Stop loss notification
       - formatTargetHit(Position, exitPrice, pnl): Target hit notification

    4. System formatting:
       - formatDailySummary(DailySummary summary): Daily trading statistics
       - formatErrorAlert(String, String, ErrorSeverity): Error notification
       - formatPositionSizeAlert(String, currentSize, maxSize, percentage): Size warning
       - formatRiskWarning(String, riskPercent): Risk notification
       - formatSystemStatus(String status): System message
       - formatMarketStatus(boolean isOpening): Market open/close notification

    5. Helper methods (reuse from TelegramMessageFormatter):
       - formatPrice(BigDecimal): Formats price to 2 decimals
       - formatCurrency(BigDecimal): Formats with emoji indicators
       - formatPercentage(BigDecimal): Formats percentage with emoji
       - formatDirection(TradeDirection): LONG/SHORT formatting
       - calculatePnLPercent methods for calculations

    Reference TelegramMessageFormatter.java for exact implementation patterns.
  </action>
  <verify>
    <automated>cd broker && mvn compile -q</automated>
  </verify>
  <done>
    SignalMessageFormatter has all formatting methods:
    - formatBuySignal, formatSellSignal, formatHoldSignal
    - formatPositionEntry, formatPositionUpdate, formatPositionExit
    - formatTradeOpen, formatTradeClose, formatStopLossHit, formatTargetHit
    - formatDailySummary, formatErrorAlert, formatPositionSizeAlert, formatRiskWarning
    - formatSystemStatus, formatMarketStatus
    - Helper methods: formatPrice, formatCurrency, formatPercentage, formatDirection
  </done>
</task>

<task type="auto">
  <name>Task 3: Create SignalConfig configuration class</name>
  <files>broker/src/main/java/com/swingtrade/broker/telegram/SignalConfig.java</files>
  <action>
    Create SignalConfig.java as a mirror of TelegramConfig.java with signal.* configuration:

    1. Class structure:
       - @Component
       - @ConfigurationProperties(prefix = "signal")
       - Nested NotifyConfig class

    2. Configuration properties:
       - enabled (boolean, default true)
       - apiUrl (String) - Signal/Signl4 webhook URL
       - apiToken (String) - Bearer token
       - chatId (String) - Single chat ID
       - notifyOnTrades (boolean, default true)
       - notifyOnSignals (boolean, default true)
       - notifyOnErrors (boolean, default false)
       - notifyOnPositions (boolean, default true)
       - maxMessageLength (int, default 4096)
       - quietHoursEnabled (boolean, default false)
       - quietHoursStart (int, default 22)
       - quietHoursEnd (int, default 7)
       - adminUserIds (List<String>)

    3. Nested NotifyConfig:
       - onTrades (boolean)
       - onSignals (boolean)
       - onErrors (boolean)
       - onPositions (boolean)
       - maxMessageLength (int)
       - rateLimitSeconds (int)

    4. Add @PostConstruct init() method that logs configuration status
    5. Provide all getters and setters
    6. Add validation in init(): warn if apiToken or chatId missing

    Reference TelegramConfig.java for exact class structure and getter/setter patterns.
  </action>
  <verify>
    <automated>cd broker && mvn compile -q</automated>
  </verify>
  <done>
    SignalConfig.java created with:
    - All signal.* configuration properties
    - Nested NotifyConfig class
    - init() method with validation
    - All getters and setters
  </done>
</task>

<task type="auto">
  <name>Task 4: Update application.properties with signal configuration</name>
  <files>broker/src/main/resources/application.properties</files>
  <action>
    Update application.properties to add signal.* configuration and update telegram.* to signal.*:

    1. Add new Signal/Signl4 configuration section:
       ```
       # =============================================================================
       # Signal/Signl4 Configuration (replacing Telegram)
       # =============================================================================
       signal.enabled=${SIGNAL_ENABLED:true}
       signal.api.url=${SIGNAL_API_URL:}
       signal.api.token=${SIGNAL_API_TOKEN:}
       signal.chat.id=${SIGNAL_CHAT_ID:}

       signal.notify.on.trades=true
       signal.notify.on.signals=true
       signal.notify.on.errors=false
       signal.notify.on.positions=true
       signal.notify.max.message.length=4096

       signal.quiet.hours.enabled=${SIGNAL_QUIET_HOURS_ENABLED:false}
       signal.quiet.hours.start=22
       signal.quiet.hours.end=7
       ```

    2. Update feature flags:
       - Change app.features.telegram-notifications.enabled=true to app.features.signal-notifications.enabled=true

    3. Keep telegram.* section as deprecated (users can migrate gradually):
       - Add comment: "# DEPRECATED - Use signal.* configuration instead"
       - Mark each property with @Deprecated comment

    4. Add documentation for required environment variables:
       - SIGNAL_API_URL: Signal/Signl4 webhook URL from dashboard
       - SIGNAL_API_TOKEN: API token from Signal/Signl4 settings
       - SIGNAL_CHAT_ID: Your Signal chat ID

    Reference Telegram configuration section for exact structure and comments.
  </action>
  <verify>
    <automated>cd broker && mvn compile -q</automated>
  </verify>
  <done>
    application.properties updated with:
    - signal.* configuration properties
    - signal api.url, api.token, chat.id configured
    - Signal notification flags (trades, signals, errors, positions)
    - Quiet hours configuration
    - telegram.* section marked as deprecated
  </done>
</task>

<task type="auto">
  <name>Task 5: Wire SignalNotificationService into BrokerNotificationIntegration</name>
  <files>broker/src/main/java/com/swingtrade/broker/telegram/BrokerNotificationIntegration.java</files>
  <action>
    Update BrokerNotificationIntegration to use SignalNotificationService for all notifications:

    1. Change dependency:
       - Keep SignalNotificationService dependency (already exists)
       - Update method calls to use full feature methods

    2. Implement all notification methods:
       - onBuySignal(Signal): Call signalService.sendBuySignal
       - onSellSignal(Signal): Call signalService.sendSellSignal
       - onHoldSignal(Signal): Call signalService.sendHoldSignal
       - onPositionEntry(Position): Call signalService.sendPositionEntry
       - onPositionUpdate(Position): Call signalService.sendPositionUpdate
       - onPositionExit(Position, Trade): Call signalService.sendPositionExit
       - onTradeOpen(Position, Signal): Call signalService.sendTradeOpen
       - onTradeClose(Position, Trade): Call signalService.sendTradeClose
       - onStopLossHit(...): Call signalService.sendStopLossHit
       - onTargetHit(...): Call signalService.sendTargetHit
       - onDailySummary(DailySummary): Call signalService.sendDailySummary
       - onError(...): Call signalService.sendErrorAlert
       - onPositionSizeAlert(...): Call signalService.sendPositionSizeAlert
       - onRiskWarning(...): Call signalService.sendRiskWarning
       - onSystemStatus(...): Call signalService.sendSystemStatus
       - onMarketStatus(...): Call signalService.sendMarketStatus

    3. Remove @Deprecated annotations from all methods

    4. Add logging for each notification type

    5. Remove BrokerService dependency if not used elsewhere
  </action>
  <verify>
    <automated>cd broker && mvn compile -q</automated>
  </verify>
  <done>
    BrokerNotificationIntegration wired to SignalNotificationService:
    - All notification methods implemented and calling SignalService
    - No @Deprecated annotations
    - Logging added for all notification events
  </done>
</task>

</tasks>

<verification>
1. Compile success:
   - cd broker && mvn compile -q succeeds

2. SignalNotificationService methods:
   - grep -c "public boolean send" broker/src/main/java/com/swingtrade/broker/telegram/SignalNotificationService.java should return 17+

3. SignalMessageFormatter methods:
   - grep -c "public String format" broker/src/main/java/com/swingtrade/broker/telegram/SignalMessageFormatter.java should return 15+

4. SignalConfig exists:
   - grep -q "ConfigurationProperties(prefix = \"signal\")" broker/src/main/java/com/swingtrade/broker/telegram/SignalConfig.java

5. application.properties has signal config:
   - grep -q "signal.api.url" broker/src/main/resources/application.properties
   - grep -q "signal.notify.on.trades" broker/src/main/resources/application.properties
</verification>

<success_criteria>
- SignalNotificationService implements all 17+ notification methods
- SignalMessageFormatter implements all 15+ formatting methods
- SignalConfig.java created with all signal.* properties
- application.properties updated with signal configuration
- BrokerNotificationIntegration wired to SignalService
- All code compiles without errors
- No @Deprecated annotations on notification methods
</success_criteria>

<output>
After completion, create `.planning/phases/09-replace-telegram-with-signal/09-01-SUMMARY.md`
</output>
