---
phase: 09-replace-telegram-with-signal
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - broker/src/main/java/com/swingtrade/broker/telegram/TelegramNotificationService.java
  - broker/src/main/java/com/swingtrade/broker/telegram/TelegramMessageFormatter.java
  - broker/src/main/java/com/swingtrade/broker/telegram/TelegramConfig.java
  - broker/src/main/java/com/swingtrade/broker/telegram/BrokerNotificationIntegration.java
  - broker/src/main/resources/application.properties
autonomous: true
requirements:
  - REQ-018
user_setup: []

must_haves:
  truths:
    - "Telegram code is deprecated but not removed for backward compatibility"
    - "System defaults to Signal notifications"
    - "Both telegram.* and signal.* properties work independently"
  artifacts:
    - path: "broker/src/main/java/com/swingtrade/broker/telegram/TelegramNotificationService.java"
      provides: "Deprecated Telegram service with @Deprecated annotation"
      min_lines: 700
    - path: "broker/src/main/java/com/swingtrade/broker/telegram/TelegramMessageFormatter.java"
      provides: "Deprecated Telegram formatter with @Deprecated annotation"
      min_lines: 800
    - path: "broker/src/main/java/com/swingtrade/broker/telegram/TelegramConfig.java"
      provides: "Deprecated Telegram config with @Deprecated annotation"
      contains: "@Deprecated"
    - path: "broker/src/main/resources/application.properties"
      provides: "Signal as default, Telegram marked deprecated"
      contains: "signal.enabled=${SIGNAL_ENABLED:true}"
  key_links:
    - from: "broker/src/main/java/com/swingtrade/broker/telegram/BrokerNotificationIntegration.java"
      to: "broker/src/main/java/com/swingtrade/broker/telegram/SignalNotificationService.java"
      via: "primary dependency"
      pattern: "private final SignalNotificationService"
    - from: "broker/src/main/resources/application.properties"
      to: "broker/src/main/java/com/swingtrade/broker/telegram/SignalConfig.java"
      via: "default configuration"
      pattern: "signal.enabled=true"

---

<objective>
Deprecate Telegram notification code by adding @Deprecated annotations, wire BrokerNotificationIntegration to use Signal by default, and update application.properties to enable Signal as the default notification channel.
</objective>

<execution_context>
/Users/kayisrahman/.claude/get-shit-done/workflows/execute-plan.md
/Users/kayisrahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
/Users/kayisrahman/Documents/workspace/ideas/swing-trade/.planning/REQUIREMENTS.md
/Users/kayisrahman/Documents/workspace/ideas/swing-trade/.planning/phases/09-replace-telegram-with-signal/09-CONTEXT.md

Prior plan: 09-01 created SignalNotificationService, SignalMessageFormatter, SignalConfig, and updated application.properties with signal configuration.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark TelegramNotificationService as @Deprecated</name>
  <files>broker/src/main/java/com/swingtrade/broker/telegram/TelegramNotificationService.java</files>
  <behavior>
    - Add @Deprecated annotation to class
    - Add class-level JavaDoc explaining deprecation and migration to Signal
    - Mark all public methods as @Deprecated with migration instructions
    - Keep all code functional for backward compatibility
  </behavior>
  <action>
    Mark TelegramNotificationService.java as deprecated:

    1. Add class-level @Deprecated annotation:
       ```java
       /**
        * @deprecated Use SignalNotificationService instead.
        *             Telegram notifications are deprecated in favor of Signal/Signl4.
        */
       @Deprecated
       @Service
       @Slf4j
       public class TelegramNotificationService {
       ```

    2. Update class JavaDoc:
       - Add migration note: "Migrate to SignalNotificationService for new implementations"
       - Add deprecation date and reason

    3. Mark notification methods as @Deprecated:
       - sendBuySignal, sendSellSignal, sendHoldSignal
       - sendPositionEntry, sendPositionUpdate, sendPositionExit
       - sendStopLossHit, sendTargetHit, sendOrderExecution
       - sendDailySummary, sendErrorAlert, sendPositionSizeAlert
       - sendRiskWarning, sendSystemStatus, sendMarketStatus
       - Each method: `@Deprecated(since = "9.0", reason = "Use SignalNotificationService instead")`

    4. Mark command handler methods as @Deprecated:
       - initializeCommandHandlers, processCommand, handleStopCommand, etc.

    5. Keep all code functional - do not remove implementations
  </action>
  <verify>
    <automated>grep -q "@Deprecated" broker/src/main/java/com/swingtrade/broker/telegram/TelegramNotificationService.java && grep -q "Use SignalNotificationService" broker/src/main/java/com/swingtrade/broker/telegram/TelegramNotificationService.java</automated>
  </verify>
  <done>
    TelegramNotificationService.java:
    - @Deprecated annotation on class and all public methods
    - Migration instructions in JavaDoc
    - All code still functional for backward compatibility
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark TelegramMessageFormatter as @Deprecated</name>
  <files>broker/src/main/java/com/swingtrade/broker/telegram/TelegramMessageFormatter.java</files>
  <behavior>
    - Add @Deprecated annotation to class
    - Add class-level JavaDoc explaining deprecation
    - Mark all format methods as @Deprecated
    - Keep all code functional for backward compatibility
  </behavior>
  <action>
    Mark TelegramMessageFormatter.java as deprecated:

    1. Add class-level @Deprecated annotation:
       ```java
       /**
        * @deprecated Use SignalMessageFormatter instead.
        *             Telegram message formatting is deprecated in favor of Signal.
        */
       @Deprecated
       @Component
       @Slf4j
       public class TelegramMessageFormatter {
       ```

    2. Update class JavaDoc:
       - Add migration note: "Migrate to SignalMessageFormatter for new implementations"
       - Note that formatting patterns are identical but Signal is preferred

    3. Mark formatting methods as @Deprecated:
       - formatBuySignal, formatSellSignal, formatHoldSignal
       - formatPositionEntry, formatPositionUpdate, formatPositionExit
       - formatTradeOpen, formatTradeClose, formatStopLossHit, formatTargetHit
       - formatDailySummary, formatErrorAlert, formatPositionSizeAlert
       - formatRiskWarning, formatSystemStatus, formatMarketStatus

    4. Keep all helper methods functional
  </action>
  <verify>
    <automated>grep -q "@Deprecated" broker/src/main/java/com/swingtrade/broker/telegram/TelegramMessageFormatter.java && grep -q "Use SignalMessageFormatter" broker/src/main/java/com/swingtrade/broker/telegram/TelegramMessageFormatter.java</automated>
  </verify>
  <done>
    TelegramMessageFormatter.java:
    - @Deprecated annotation on class and all public methods
    - Migration instructions in JavaDoc
    - All code still functional for backward compatibility
  </done>
</task>

<task type="auto">
  <name>Task 3: Mark TelegramConfig as @Deprecated</name>
  <files>broker/src/main/java/com/swingtrade/broker/telegram/TelegramConfig.java</files>
  <behavior>
    - Add @Deprecated annotation to class
    - Add class-level JavaDoc explaining deprecation
    - Keep all code functional for backward compatibility
  </behavior>
  <action>
    Mark TelegramConfig.java as deprecated:

    1. Add class-level @Deprecated annotation:
       ```java
       /**
        * @deprecated Use SignalConfig instead.
        *             Telegram configuration is deprecated in favor of Signal.
        */
       @Deprecated
       @Component
       @ConfigurationProperties(prefix = "telegram")
       public class TelegramConfig {
       ```

    2. Update class JavaDoc:
       - Add migration note: "Migrate to signal.* configuration properties"
       - Provide example of signal.* equivalent properties

    3. Keep all getter/setter methods functional
  </action>
  <verify>
    <automated>grep -q "@Deprecated" broker/src/main/java/com/swingtrade/broker/telegram/TelegramConfig.java && grep -q "Use SignalConfig" broker/src/main/java/com/swingtrade/broker/telegram/TelegramConfig.java</automated>
  </verify>
  <done>
    TelegramConfig.java:
    - @Deprecated annotation on class
    - Migration instructions in JavaDoc
    - All configuration properties still functional
  </done>
</task>

<task type="auto">
  <name>Task 4: Update BrokerNotificationIntegration to use Signal by default</name>
  <files>broker/src/main/java/com/swingtrade/broker/telegram/BrokerNotificationIntegration.java</files>
  <behavior>
    - Add SignalNotificationService as primary dependency
    - Wire all methods to use SignalService
    - Keep Telegram deprecation but not as primary
    - Remove @Deprecated from methods
  </behavior>
  <action>
    Update BrokerNotificationIntegration.java:

    1. Add SignalNotificationService as primary dependency:
       ```java
       @Component
       @RequiredArgsConstructor
       @Slf4j
       public class BrokerNotificationIntegration {
           private final SignalNotificationService signalService;
           private final SignalMessageFormatter signalFormatter;
       ```

    2. Wire all notification methods to SignalService:
       - onBuySignal, onSellSignal, onHoldSignal
       - onPositionEntry, onPositionUpdate, onPositionExit
       - onTradeOpen, onTradeClose, onStopLossHit, onTargetHit
       - onDailySummary, onError, onPositionSizeAlert, onRiskWarning
       - onSystemStatus, onMarketStatus

    3. Remove @Deprecated annotations from all methods

    4. Add logging for each notification:
       - DEBUG for signal notifications
       - INFO for trade events
       - WARN for risk warnings
       - ERROR for errors

    5. Update class JavaDoc:
       - State that Signal is the primary notification service
       - Note Telegram is deprecated and Signal is the default
  </action>
  <verify>
    <automated>cd broker && mvn compile -q</automated>
  </verify>
  <done>
    BrokerNotificationIntegration wired to Signal:
    - SignalNotificationService as primary dependency
    - All notification methods call SignalService
    - No @Deprecated annotations
    - Proper logging for all notification types
  </done>
</task>

<task type="auto">
  <name>Task 5: Update application.properties to enable Signal by default</name>
  <files>broker/src/main/resources/application.properties</files>
  <behavior>
    - Set signal.enabled=true as default
    - Mark telegram.* section as DEPRECATED
    - Add migration guide comments
  </behavior>
  <action>
    Update application.properties:

    1. Ensure Signal section is complete:
       ```
       # =============================================================================
       # Signal/Signl4 Configuration (primary notification service)
       # =============================================================================
       signal.enabled=${SIGNAL_ENABLED:true}
       signal.api.url=${SIGNAL_API_URL:}
       signal.api.token=${SIGNAL_API_TOKEN:}
       signal.chat.id=${SIGNAL_CHAT_ID:}

       signal.notify.on.trades=true
       signal.notify.on.signals=true
       signal.notify.on.errors=false
       signal.notify.on.positions=true
       signal.notify.max.message.length=4096

       signal.quiet.hours.enabled=${SIGNAL_QUIET_HOURS_ENABLED:false}
       signal.quiet.hours.start=22
       signal.quiet.hours.end=7
       ```

    2. Mark Telegram section as DEPRECATED:
       ```
       # =============================================================================
       # Telegram Configuration (DEPRECATED - Use Signal/Signl4 instead)
       # =============================================================================
       # WARNING: Telegram notifications are deprecated.
       # Migrate to signal.* configuration properties.
       # See: Signal/Signl4 Configuration section above.
       # =============================================================================
       telegram.bot.enabled=${TELEGRAM_BOT_ENABLED:false}
       telegram.bot.token=${TELEGRAM_BOT_TOKEN:}
       telegram.chat.ids=${TELEGRAM_CHAT_IDS:}
       ...
       ```

    3. Update feature flags:
       - app.features.signal-notifications.enabled=true
       - app.features.telegram-notifications.enabled=false (or deprecated comment)

    4. Add migration guide comment block:
       - Explain migration from telegram.* to signal.*
       - Provide environment variable mappings
  </action>
  <verify>
    <automated>grep -q "SIGNAL_ENABLED:true" broker/src/main/resources/application.properties && grep -q "DEPRECATED" broker/src/main/resources/application.properties</automated>
  </verify>
  <done>
    application.properties updated:
    - Signal enabled=true as default
    - Telegram section marked DEPRECATED
    - Migration guide comments added
    - Feature flags updated
  </done>
</task>

</tasks>

<verification>
1. Compile success:
   - cd broker && mvn compile -q succeeds

2. Telegram services marked deprecated:
   - grep -c "@Deprecated" broker/src/main/java/com/swingtrade/broker/telegram/TelegramNotificationService.java should return 15+
   - grep -c "@Deprecated" broker/src/main/java/com/swingtrade/broker/telegram/TelegramMessageFormatter.java should return 15+
   - grep -c "@Deprecated" broker/src/main/java/com/swingtrade/broker/telegram/TelegramConfig.java should return 1

3. BrokerNotificationIntegration uses Signal:
   - grep -q "private final SignalNotificationService" broker/src/main/java/com/swingtrade/broker/telegram/BrokerNotificationIntegration.java
   - grep -c "@Deprecated" broker/src/main/java/com/swingtrade/broker/telegram/BrokerNotificationIntegration.java should return 0

4. application.properties has correct defaults:
   - grep -q "signal.enabled=true" broker/src/main/resources/application.properties
   - grep -q "DEPRECATED" broker/src/main/resources/application.properties
   - grep -q "TELEGRAM_BOT_ENABLED:false" broker/src/main/resources/application.properties
</verification>

<success_criteria>
- TelegramNotificationService marked @Deprecated with migration instructions
- TelegramMessageFormatter marked @Deprecated with migration instructions
- TelegramConfig marked @Deprecated with migration instructions
- BrokerNotificationIntegration uses SignalNotificationService by default
- No @Deprecated on BrokerNotificationIntegration methods
- application.properties sets signal.enabled=true, telegram.enabled=false
- All code compiles without errors
- Backward compatibility maintained (Telegram code still works if enabled)
</success_criteria>

<output>
After completion, create `.planning/phases/09-replace-telegram-with-signal/09-02-SUMMARY.md`
</output>
