---
phase: 09-replace-telegram-with-signal
plan: 03
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - broker/src/test/java/com/swingtrade/broker/telegram/SignalNotificationServiceTest.java
  - broker/src/test/java/com/swingtrade/broker/telegram/SignalMessageFormatterTest.java
  - broker/src/test/java/com/swingtrade/broker/telegram/SignalConfigTest.java
  - broker/src/test/java/com/swingtrade/broker/telegram/BrokerNotificationIntegrationTest.java
autonomous: true
requirements:
  - REQ-018
user_setup: []


# WORKTREE WORKFLOW ENFORCEMENT
# CRITICAL: This plan MUST be executed in a git worktree environment
# Root .planning/ is source of truth - worktree .planning/ is working copy
worktree_enforcement:
  required: true
  reason: "Prevents direct edits to root .planning/ on main branch"
  workflow:
    - step: 1
      action: "Verify worktree directory"
      command: "pwd | grep worktrees"
      fail_message: "ERROR: Must be in a worktree directory (e.g., .claude/worktrees/phase-05/)"
    - step: 2
      action: "Verify worktree branch"
      command: "git branch --show-current"
      expected_pattern: "worktree-phase-.*"
      fail_message: "ERROR: Must be on a worktree branch (e.g., worktree-phase-05)"
    - step: 3
      action: "Edit planning docs in worktree"
      path: ".planning/phases/09-replace-telegram-with-signal/"
      note: "Do NOT edit .planning/ in root repository"
    - step: 4
      action: "Sync to root before merge"
      command: "Skill(\"superpowers:gsd-worktree-workflow --sync-to-root\")"
      when: "Before merging worktree branch to main"
    - step: 5
      action: "Verify before merge"
      command: "gsd:verify"
      when: "After plan completion, before merge"

must_haves:
  truths:
    - "All Signal notification methods have unit tests"
    - "All Signal message formatting methods have unit tests"
    - "SignalConfig validation works correctly"
    - "BrokerNotificationIntegration routes to SignalService correctly"
  artifacts:
    - path: "broker/src/test/java/com/swingtrade/broker/telegram/SignalNotificationServiceTest.java"
      provides: "Unit tests for SignalNotificationService"
      min_lines: 300
    - path: "broker/src/test/java/com/swingtrade/broker/telegram/SignalMessageFormatterTest.java"
      provides: "Unit tests for SignalMessageFormatter"
      min_lines: 400
    - path: "broker/src/test/java/com/swingtrade/broker/telegram/SignalConfigTest.java"
      provides: "Unit tests for SignalConfig"
      min_lines: 100
    - path: "broker/src/test/java/com/swingtrade/broker/telegram/BrokerNotificationIntegrationTest.java"
      provides: "Integration tests for BrokerNotificationIntegration"
      min_lines: 200
  key_links:
    - from: "broker/src/test/java/com/swingtrade/broker/telegram/SignalNotificationServiceTest.java"
      to: "broker/src/test/java/com/swingtrade/broker/telegram/SignalMessageFormatterTest.java"
      via: "shared test fixtures"
      pattern: "TestHelper|Fixture"
    - from: "broker/src/test/java/com/swingtrade/broker/telegram/BrokerNotificationIntegrationTest.java"
      to: "broker/src/test/java/com/swingtrade/broker/telegram/SignalNotificationServiceTest.java"
      via: "Mockito mocks"
      pattern: "@Mock SignalNotificationService"

---

<objective>
Add comprehensive unit tests for Signal notification service, message formatter, configuration, and integration layer. Ensure all notification methods are tested with proper mocking.
</objective>

<execution_context>
/Users/kayisrahman/.claude/get-shit-done/workflows/execute-plan.md
/Users/kayisrahman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
/Users/kayisrahman/Documents/workspace/ideas/swing-trade/.planning/REQUIREMENTS.md
/Users/kayisrahman/Documents/workspace/ideas/swing-trade/.planning/phases/09-replace-telegram-with-signal/09-CONTEXT.md

Prior plans:
- 09-01: Created SignalNotificationService, SignalMessageFormatter, SignalConfig, updated application.properties
- 09-02: Deprecated Telegram services, wired BrokerNotificationIntegration to Signal
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SignalMessageFormatterTest</name>
  <files>broker/src/test/java/com/swingtrade/broker/telegram/SignalMessageFormatterTest.java</files>
  <behavior>
    - Test all format methods return expected string patterns
    - Test emoji presence in output
    - Test price/currency/percentage formatting helpers
    - Test null handling
  </behavior>
  <action>
    Create SignalMessageFormatterTest.java with comprehensive tests:

    1. Signal formatting tests:
       - testFormatBuySignal(): Verifies BUY emoji, symbol, confidence, reasoning
       - testFormatSellSignal(): Verifies SELL emoji, symbol, confidence
       - testFormatHoldSignal(): Verifies HOLD emoji, symbol, reasoning

    2. Position formatting tests:
       - testFormatPositionEntry(): Verifies position entry details, quantity, prices
       - testFormatPositionUpdate(): Verifies current price, P&L
       - testFormatPositionExit(): Verifies exit details, P&L, duration

    3. Trade formatting tests:
       - testFormatTradeOpen(): Verifies trade open with signal details
       - testFormatTradeClose(): Verifies trade close with P&L indicators
       - testFormatStopLossHit(): Verifies SL hit notification
       - testFormatTargetHit(): Verifies target hit notification

    4. System formatting tests:
       - testFormatDailySummary(): Verifies daily statistics format
       - testFormatErrorAlert(): Verifies error severity emoji
       - testFormatPositionSizeAlert(): Verifies size warning
       - testFormatRiskWarning(): Verifies risk percentage
       - testFormatSystemStatus(): Verifies system message
       - testFormatMarketStatus(): Verifies open/close messages

    5. Helper method tests:
       - testFormatPrice(): Verifies decimal formatting
       - testFormatCurrency(): Verifies emoji indicators (+/-)
       - testFormatPercentage(): Verifies percentage formatting
       - testFormatDirection(): Verifies LONG/SHORT

    6. Edge case tests:
       - testFormatBuySignalNullSignal(): Verifies null safety
       - testFormatTradeCloseNullTrade(): Verifies null handling
       - testFormatPriceNull(): Verifies "N/A" output

    Use Mockito and AssertJ for assertions.
    Create test fixtures for Signal, Position, Trade objects.
  </action>
  <verify>
    <automated>cd broker && mvn test -Dtest=SignalMessageFormatterTest -q</automated>
  </verify>
  <done>
    SignalMessageFormatterTest.java:
    - 15+ format method tests
    - 4+ helper method tests
    - 5+ edge case tests
    - All tests passing
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SignalNotificationServiceTest</name>
  <files>broker/src/test/java/com/swingtrade/broker/telegram/SignalNotificationServiceTest.java</files>
  <behavior>
    - Test all notification methods call correct formatter methods
    - Test quiet hours logic
    - Test message truncation
    - Test configuration validation
  </behavior>
  <action>
    Create SignalNotificationServiceTest.java with comprehensive tests:

    1. Signal notification tests:
       - testSendBuySignal(): Verifies formatBuySignal called, sendMessage called
       - testSendSellSignal(): Verifies formatSellSignal called
       - testSendHoldSignal(): Verifies formatHoldSignal called

    2. Position notification tests:
       - testSendPositionEntry(): Verifies formatPositionEntry called
       - testSendPositionUpdate(): Verifies formatPositionUpdate called
       - testSendPositionExit(): Verifies formatPositionExit called

    3. Trade notification tests:
       - testSendTradeOpen(): Verifies formatTradeOpen called
       - testSendTradeClose(): Verifies formatTradeClose called
       - testSendStopLossHit(): Verifies formatStopLossHit called
       - testSendTargetHit(): Verifies formatTargetHit called

    4. System notification tests:
       - testSendDailySummary(): Verifies formatDailySummary called
       - testSendErrorAlert(): Verifies formatErrorAlert called with severity
       - testSendPositionSizeAlert(): Verifies formatPositionSizeAlert called
       - testSendRiskWarning(): Verifies formatRiskWarning called
       - testSendSystemStatus(): Verifies formatSystemStatus called
       - testSendMarketStatus(): Verifies formatMarketStatus called

    5. Configuration tests:
       - testDisabledNotifications(): Verifies returns false when disabled
       - testMissingChatId(): Verifies warning logged, returns false
       - testMissingApiToken(): Verifies warning logged, returns false

    6. Quiet hours tests:
       - testQuietHoursEnabled(): Verifies messages skipped during quiet hours
       - testQuietHoursDisabled(): Verifies messages sent when disabled
       - testOvernightQuietHours(): Verifies 22:00-07:00 logic

    7. Message truncation tests:
       - testMessageTruncation(): Verifies messages > max length are truncated

    Use Mockito to mock SignalMessageFormatter and verify interactions.
  </action>
  <verify>
    <automated>cd broker && mvn test -Dtest=SignalNotificationServiceTest -q</automated>
  </verify>
  <done>
    SignalNotificationServiceTest.java:
    - 17+ notification method tests
    - 5+ configuration tests
    - 3+ quiet hours tests
    - 1+ truncation test
    - All tests passing
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create SignalConfigTest</name>
  <files>broker/src/test/java/com/swingtrade/broker/telegram/SignalConfigTest.java</files>
  <behavior>
    - Test configuration property binding
    - Test nested NotifyConfig binding
    - Test default values
  </behavior>
  <action>
    Create SignalConfigTest.java with configuration tests:

    1. Property binding tests:
       - testEnabledDefaultsTrue(): Verifies enabled defaults to true
       - testApiUrlBinds(): Verifies signal.api.url binding
       - testApiTokenBinds(): Verifies signal.api.token binding
       - testChatIdBinds(): Verifies signal.chat.id binding

    2. Notification flag tests:
       - testNotifyOnTradesDefaultsTrue(): Verifies trades enabled
       - testNotifyOnSignalsDefaultsTrue(): Verifies signals enabled
       - testNotifyOnErrorsDefaultsFalse(): Verifies errors disabled
       - testNotifyOnPositionsDefaultsTrue(): Verifies positions enabled

    3. Quiet hours tests:
       - testQuietHoursDefaultsDisabled(): Verifies quiet hours disabled by default
       - testQuietHoursStartDefaults22(): Verifies start hour 22
       - testQuietHoursEndDefaults7(): Verifies end hour 7

    4. NotifyConfig nested tests:
       - testNestedNotifyOnTrades(): Verifies nested configuration binds
       - testNestedMaxMessageLength(): Verifies nested maxMessageLength

    5. List binding tests:
       - testAdminUserIdsBinds(): Verifies List<String> binding
       - testEmptyAdminUserIds(): Verifies empty list default

    Use @ConfigurationPropertiesBindTest or @SpringBootTest with @TestPropertySource.
  </action>
  <verify>
    <automated>cd broker && mvn test -Dtest=SignalConfigTest -q</automated>
  </verify>
  <done>
    SignalConfigTest.java:
    - 8+ property binding tests
    - 3+ quiet hours tests
    - 2+ nested config tests
    - All tests passing
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create BrokerNotificationIntegrationTest</name>
  <files>broker/src/test/java/com/swingtrade/broker/telegram/BrokerNotificationIntegrationTest.java</files>
  <behavior>
    - Test all notification methods route to SignalService correctly
    - Test integration wiring
    - Test isEnabled returns SignalService status
  </behavior>
  <action>
    Create BrokerNotificationIntegrationTest.java with integration tests:

    1. Signal routing tests:
       - testOnBuySignalRoutesToService(): Verifies signalService.sendBuySignal called
       - testOnSellSignalRoutesToService(): Verifies signalService.sendSellSignal called
       - testOnHoldSignalRoutesToService(): Verifies signalService.sendHoldSignal called

    2. Position routing tests:
       - testOnPositionEntryRoutesToService(): Verifies signalService.sendPositionEntry called
       - testOnPositionUpdateRoutesToService(): Verifies signalService.sendPositionUpdate called
       - testOnPositionExitRoutesToService(): Verifies signalService.sendPositionExit called

    3. Trade routing tests:
       - testOnTradeOpenRoutesToService(): Verifies signalService.sendTradeOpen called
       - testOnTradeCloseRoutesToService(): Verifies signalService.sendTradeClose called
       - testOnStopLossHitRoutesToService(): Verifies signalService.sendStopLossHit called
       - testOnTargetHitRoutesToService(): Verifies signalService.sendTargetHit called

    4. System routing tests:
       - testOnDailySummaryRoutesToService(): Verifies signalService.sendDailySummary called
       - testOnErrorRoutesToService(): Verifies signalService.sendErrorAlert called
       - testOnPositionSizeAlertRoutesToService(): Verifies signalService.sendPositionSizeAlert called
       - testOnRiskWarningRoutesToService(): Verifies signalService.sendRiskWarning called
       - testOnSystemStatusRoutesToService(): Verifies signalService.sendSystemStatus called
       - testOnMarketStatusRoutesToService(): Verifies signalService.sendMarketStatus called

    5. Configuration tests:
       - testIsNotificationsEnabled(): Verifies delegates to signalService.isEnabled
       - testGetChatCount(): Verifies delegates to signalService.getChatCount
       - testShouldNotifyOnTrades(): Verifies delegates to signalService.shouldNotifyOnTrades

    6. Logging tests:
       - testOnStopLossHitLogsWarning(): Verifies WARN level logged
       - testOnTargetHitLogsInfo(): Verifies INFO level logged

    Use @Mock SignalNotificationService, @Mock SignalMessageFormatter.
    Use @InjectMocks for BrokerNotificationIntegration.
  </action>
  <verify>
    <automated>cd broker && mvn test -Dtest=BrokerNotificationIntegrationTest -q</automated>
  </verify>
  <done>
    BrokerNotificationIntegrationTest.java:
    - 16+ routing tests
    - 3+ configuration tests
    - 2+ logging tests
    - All tests passing
  </done>
</task>

<task type="auto">
  <name>Task 5: Run all notification tests and verify coverage</name>
  <files>broker/src/test/java/com/swingtrade/broker/telegram/*.java</files>
  <behavior>
    - Run all test classes together
    - Verify all tests pass
    - Generate coverage report
  </behavior>
  <action>
    Execute comprehensive test suite:

    1. Run all notification tests:
       - cd broker && mvn test -Dtest='**/telegram/*Test'

    2. Verify test counts:
       - SignalMessageFormatterTest: 20+ tests
       - SignalNotificationServiceTest: 25+ tests
       - SignalConfigTest: 13+ tests
       - BrokerNotificationIntegrationTest: 21+ tests

    3. Generate coverage report:
       - cd broker && mvn jacoco:report
       - Verify broker/src/test/java/com/swingtrade/broker/telegram coverage

    4. Fix any failing tests
    5. Document test coverage results
  </action>
  <verify>
    <automated>cd broker && mvn test -Dtest='**/telegram/*Test' -q && echo 'All notification tests passed'</automated>
  </verify>
  <done>
    All notification tests passing:
    - SignalMessageFormatterTest: 20+ tests
    - SignalNotificationServiceTest: 25+ tests
    - SignalConfigTest: 13+ tests
    - BrokerNotificationIntegrationTest: 21+ tests
    - Total: 80+ tests
  </done>
</task>

</tasks>

<verification>
1. All test files exist:
   - ls broker/src/test/java/com/swingtrade/broker/telegram/*Test.java

2. All tests compile and pass:
   - cd broker && mvn test -Dtest='**/telegram/*Test' -q

3. Test count verification:
   - grep -c "void test" broker/src/test/java/com/swingtrade/broker/telegram/SignalMessageFormatterTest.java
   - grep -c "void test" broker/src/test/java/com/swingtrade/broker/telegram/SignalNotificationServiceTest.java
   - grep -c "void test" broker/src/test/java/com/swingtrade/broker/telegram/SignalConfigTest.java
   - grep -c "void test" broker/src/test/java/com/swingtrade/broker/telegram/BrokerNotificationIntegrationTest.java

4. Coverage report generated:
   - ls broker/target/site/jacoco/index.html
</verification>

<success_criteria>
- SignalMessageFormatterTest created with 20+ tests
- SignalNotificationServiceTest created with 25+ tests
- SignalConfigTest created with 13+ tests
- BrokerNotificationIntegrationTest created with 21+ tests
- All 80+ tests pass
- Test coverage report generated
- No compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-replace-telegram-with-signal/09-03-SUMMARY.md`
</output>
