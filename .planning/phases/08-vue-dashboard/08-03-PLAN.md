---
phase: 08-vue-dashboard
plan: 03
type: execute
wave: 2
depends_on: ['08-02']
files_modified:
  - swing-trade-dashboard/vite.config.ts
  - swing-trade-dashboard/tsconfig.json
  - swing-trade-dashboard/src/main.ts
  - swing-trade-dashboard/src/views/NotFoundView.vue
  - swing-trade-dashboard/src/components/LoadingSpinner.vue
  - swing-trade-dashboard/src/components/ErrorMessage.vue
  - swing-trade-dashboard/.env.example
  - swing-trade-dashboard/README.md
autonomous: false
requirements: [DASH-06, DASH-07]
user_setup:
  - service: none
    why: "No external services required"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "Application shows loading state while fetching data from backend"
    - "Application shows user-friendly error messages when API fails"
    - "Production build completes successfully with minified assets"
    - "All views are responsive on mobile, tablet, and desktop"
    - "Application handles unknown routes with 404 page"
    - "Environment variables properly configured for API endpoints"
  artifacts:
    - path: "swing-trade-dashboard/src/components/LoadingSpinner.vue"
      provides: "Loading indicator component"
      min_lines: 20
    - path: "swing-trade-dashboard/src/components/ErrorMessage.vue"
      provides: "Error display component"
      min_lines: 25
    - path: "swing-trade-dashboard/src/views/NotFoundView.vue"
      provides: "404 not found page"
      min_lines: 30
    - path: "swing-trade-dashboard/.env.example"
      provides: "Environment variable template"
      contains: "VITE_API_BASE_URL"
    - path: "swing-trade-dashboard/src/main.ts"
      provides: "Vue app entry with production config"
      contains: "createApp|mount|vite-plugin-inspect"
    - path: "swing-trade-dashboard/vite.config.ts"
      provides: "Production build configuration"
      contains: "build|rollupOptions|terser"
  key_links:
    - from: "swing-trade-dashboard/src/views/DashboardView.vue"
      to: "swing-trade-dashboard/src/components/LoadingSpinner.vue"
      via: "v-if loading state"
      pattern: "loadingSpinner"
    - from: "swing-trade-dashboard/src/views/DashboardView.vue"
      to: "swing-trade-dashboard/src/components/ErrorMessage.vue"
      via: "v-if error state"
      pattern: "errorMessage"
    - from: "swing-trade-dashboard/src/views/PositionsView.vue"
      to: "swing-trade-dashboard/src/components/LoadingSpinner.vue"
      via: "v-if loading state"
      pattern: "loadingSpinner"
    - from: "swing-trade-dashboard/src/views/PositionsView.vue"
      to: "swing-trade-dashboard/src/components/ErrorMessage.vue"
      via: "v-if error state"
      pattern: "errorMessage"
    - from: "swing-trade-dashboard/src/views/SignalsView.vue"
      to: "swing-trade-dashboard/src/components/LoadingSpinner.vue"
      via: "v-if loading state"
      pattern: "loadingSpinner"
    - from: "swing-trade-dashboard/src/views/SignalsView.vue"
      to: "swing-trade-dashboard/src/components/ErrorMessage.vue"
      via: "v-if error state"
      pattern: "errorMessage"
    - from: "swing-trade-dashboard/src/views/PortfolioView.vue"
      to: "swing-trade-dashboard/src/components/LoadingSpinner.vue"
      via: "v-if loading state"
      pattern: "loadingSpinner"
    - from: "swing-trade-dashboard/src/views/PortfolioView.vue"
      to: "swing-trade-dashboard/src/components/ErrorMessage.vue"
      via: "v-if error state"
      pattern: "errorMessage"

---

<objective>
Add production-ready features to the Vue dashboard including loading states, error handling, production build configuration, and responsive design verification.

Purpose: Transform the dashboard from a working prototype to a production-ready monitoring application with proper error handling, loading states, 404 handling, and optimized production builds.

Output: Production build configuration, loading/error components, 404 page, environment configuration, and verified responsive design.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-vue-dashboard/08-02-SUMMARY.md
@.planning/phases/08-vue-dashboard/08-VUE-DASHBOARD-UI-SPEC.md
@.planning/phases/08-vue-dashboard/08-RESEARCH.md

# Existing Implementation Context

## Current vite.config.ts
```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
      },
    },
  },
})
```

## Current tsconfig.json
- Uses module: "ESNext" for imports
- Uses moduleResolution: "bundler"
- No strict mode enabled (needs update)
- No esModuleInterop (needs update)

## Current main.ts
```typescript
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import './assets/main.css'

createApp(App).use(router).mount('#app')
```

## API Config (current)
- API_BASE_URL: 'http://localhost:8080/api'
- REQUEST_TIMEOUT: 30000

## Existing components from Phase 08-02
- MetricCard.vue, PositionCard.vue, SignalCard.vue
- PerformanceMetrics.vue, DashboardView.vue
- PositionsView.vue, SignalsView.vue, PortfolioView.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoadingSpinner.vue - Reusable loading indicator</name>
  <files>swing-trade-dashboard/src/components/LoadingSpinner.vue</files>
  <action>
Create LoadingSpinner.vue - a reusable loading indicator component:

1. Component props:
   - size (string): 'sm' | 'md' | 'lg' (default: 'md')
   - message (string): Optional loading message
   - fullScreen (boolean): Full screen overlay (default: false)

2. Template structure:
   - Loading spinner using CSS animation
   - Message below spinner (optional)
   - Full screen overlay option with z-9999

3. Size variants:
   - sm: w-4 h-4 border-2 (16px)
   - md: w-8 h-8 border-3 (32px)
   - lg: w-12 h-12 border-4 (48px)

4. Styling:
   - Border: indigo-600 border-t-transparent for spinner
   - Message: text-sm text-gray-500 dark:text-gray-400
   - Full screen: fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center

5. CSS animation:
   - @keyframes spin { to { transform: rotate(360deg) } }
   - Animation duration: 1s linear infinite

6. Export as Vue 3 SFC
  </action>
  <verify>
    <automated>grep -c "defineProps\|size" swing-trade-dashboard/src/components/LoadingSpinner.vue</automated>
  </verify>
  <done>LoadingSpinner.vue created with size variants, message, full screen option</done>
</task>

<task type="auto">
  <name>Task 2: Create ErrorMessage.vue - Reusable error display component</name>
  <files>swing-trade-dashboard/src/components/ErrorMessage.vue</files>
  <action>
Create ErrorMessage.vue - a reusable error display component:

1. Component props:
   - message (string): Error message to display
   - detail (string): Optional detailed error message
   - showRetry (boolean): Show retry button (default: false)
   - onRetry (function): Retry callback when button clicked

2. Template structure:
   - Error container with error-500 background
   - Error icon (X circle SVG)
   - Error title: "Error" with text-error-600
   - Error message: text-gray-700 dark:text-gray-300
   - Retry button (optional): indigo-600 primary button

3. Styling:
   - rounded-2xl border border-error-200 dark:border-error-900
   - p-6 for padding
   - Dark mode: dark:bg-error-900/20

4. Error icon: h-12 w-12 text-error-500

5. Empty state handling: Show "No error details available" if message is empty
  </action>
  <verify>
    <automated>grep -c "defineProps\|message" swing-trade-dashboard/src/components/ErrorMessage.vue</automated>
  </verify>
  <done>ErrorMessage.vue created with message, detail, retry button</done>
</task>

<task type="auto">
  <name>Task 3: Create NotFoundView.vue - 404 not found page</name>
  <files>swing-trade-dashboard/src/views/NotFoundView.vue</files>
  <action>
Create NotFoundView.vue - a 404 not found page:

1. Template structure:
   - Centered container with flex flex-col items-center justify-center
   - Large 404 text with text-9xl font-bold text-gray-200 dark:text-gray-800
   - Title: "Page Not Found" with text-2xl font-bold text-gray-800 dark:text-white
   - Description: "The page you are looking for doesn't exist" with text-gray-500
   - Home button: primary indigo-600 button linking to /

2. Styling:
   - Full screen height: h-screen
   - Proper dark mode support
   - 8-point spacing scale

3. Router link to home: /
  </action>
  <verify>
    <automated>grep -c "router-link\|/" swing-trade-dashboard/src/views/NotFoundView.vue</automated>
  </verify>
  <done>NotFoundView.vue created with 404 display, home button</done>
</task>

<task type="auto">
  <name>Task 4: Update main.ts - Add proper app configuration</name>
  <files>swing-trade-dashboard/src/main.ts</files>
  <action>
Update main.ts with production-ready Vue app configuration:

1. Keep existing imports:
   - createApp from 'vue'
   - App from './App.vue'
   - router from './router'
   - './assets/main.css'

2. Add Vue config:
   - config.globalProperties.$debug = false (disable debug in production)
   - config.warnHandler = null (disable warnings in production)

3. Create app with proper mounting:
   - createApp(App)
   - .use(router)
   - .mount('#app')

4. Add error handler for production:
   - app.config.errorHandler = (err, vm, info) => console.error(err, info)

5. Keep development-only logging if needed
  </action>
  <verify>
    <automated>grep -c "createApp\|use\|mount" swing-trade-dashboard/src/main.ts</automated>
  </verify>
  <done>main.ts updated with error handler and production config</done>
</task>

<task type="auto">
  <name>Task 5: Update vite.config.ts - Add production build configuration</name>
  <files>swing-trade-dashboard/vite.config.ts</files>
  <action>
Update vite.config.ts with production build optimization:

1. Keep existing Vue plugin:
   - plugins: [vue()]

2. Add production build config:
   - rollupOptions: { output: { manualChunks: { vendor: ['vue', 'vue-router', 'pinia', 'axios'] } } }

3. Add minifier:
   - terserOptions: { compress: { drop_console: true, drop_debugger: true } }

4. Add sourcemap option:
   - sourcemap: process.env.NODE_ENV === 'development'

5. Add build target:
   - target: 'es2020'

6. Add preview server config:
   - preview: { port: 4173, host: true }

7. Add CSS optimization:
   - cssMinifier: 'lightningcss'

8. Keep existing server config for development
  </action>
  <verify>
    <automated>grep -c "build\|rollupOptions\|terser" swing-trade-dashboard/vite.config.ts</automated>
  </verify>
  <done>vite.config.ts updated with minification, code splitting, sourcemap</done>
</task>

<task type="auto">
  <name>Task 6: Update tsconfig.json - Add strict TypeScript settings</name>
  <files>swing-trade-dashboard/tsconfig.json</files>
  <action>
Update tsconfig.json with strict TypeScript settings:

1. Keep existing compilerOptions:
   - lib: ['ES2020', 'DOM', 'DOM.Iterable']
   - module: 'ESNext'
   - moduleResolution: 'bundler'
   - resolveJsonModule: true
   - isolatedModules: true
   - noEmit: true
   - skipLibCheck: true

2. Add strict settings:
   - strict: true
   - noUnusedLocals: true
   - noUnusedParameters: true
   - noFallthroughCasesInSwitch: true

3. Add esModuleInterop:
   - esModuleInterop: true
   - allowSyntheticDefaultImports: true

4. Add forceConsistentCasingInFileNames: true

5. Keep include: ['src/**/*.ts', 'src/**/*.vue', 'src/**/*.tsx', 'src/**/*.jsx']
  </action>
  <verify>
    <automated>grep -c "strict\|noUnusedLocals\|esModuleInterop" swing-trade-dashboard/tsconfig.json</automated>
  </verify>
  <done>tsconfig.json updated with strict mode and proper module settings</done>
</task>

<task type="auto">
  <name>Task 7: Create .env.example - Environment variable template</name>
  <files>swing-trade-dashboard/.env.example</files>
  <action>
Create .env.example with all required environment variables:

1. API Configuration:
   ```
   VITE_API_BASE_URL=http://localhost:8080/api
   VITE_API_TIMEOUT=30000
   ```

2. Feature Flags:
   ```
   VITE_ENABLE_DEBUG=false
   VITE_ENABLE_ANALYTICS=false
   ```

3. Build Configuration:
   ```
   VITE_BUILD_MINIFY=true
   VITE_BUILD_SOURCEMAP=false
   ```

4. Documentation comments:
   - Explain each variable
   - Note development vs production usage
   - Reference to config.ts usage

5. Add warning:
   ```
   # DO NOT commit .env file with secrets
   # Copy this file to .env and update values for your environment
   ```

6. Add example for different environments:
   ```
   # Development
   VITE_API_BASE_URL=http://localhost:8080/api
   
   # Staging
   # VITE_API_BASE_URL=https://staging-api.example.com/api
   
   # Production
   # VITE_API_BASE_URL=https://api.example.com/api
   ```
  </action>
  <verify>
    <automated>grep -c "VITE_API_BASE_URL\|VITE_API_TIMEOUT" swing-trade-dashboard/.env.example</automated>
  </verify>
  <done>.env.example created with API configuration, feature flags, and examples</done>
</task>

<task type="auto">
  <name>Task 8: Update README.md - Project documentation</name>
  <files>swing-trade-dashboard/README.md</files>
  <action>
Update README.md with comprehensive project documentation:

1. Project Title and Description:
   - SwingTrade Dashboard
   - Vue 3 monitoring application for swing trading system
   - Real-time positions, signals, and portfolio monitoring

2. Features:
   - Real-time market overview
   - Positions management and monitoring
   - Signal generation and filtering
   - Portfolio performance tracking
   - Dark mode support
   - Responsive design

3. Tech Stack:
   - Vue 3 + TypeScript
   - Vite build tool
   - Tailwind CSS
   - Vue Router
   - Pinia
   - Axios

4. Development:
   - npm install
   - npm run dev
   - npm run build
   - npm run preview

5. Environment Variables:
   - Reference .env.example
   - API configuration

6. Production Deployment:
   - Build output in dist/
   - Static file serving
   - Router configuration (hash history)

7. Browser Support:
   - Chrome/Edge (latest)
   - Firefox (latest)
   - Safari (latest)

8. License: MIT or proprietary
  </action>
  <verify>
    <automated>grep -c "Vue 3\|Vite\|Tailwind" swing-trade-dashboard/README.md</automated>
  </verify>
  <done>README.md created with features, tech stack, development instructions</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 9: Verify responsive design across breakpoints</name>
  <files>swing-trade-dashboard/src/**/*.vue</files>
  <action>
Verify responsive design across all breakpoints:

1. Start dev server: cd swing-trade-dashboard && npm run dev
2. Open http://localhost:5173 in browser
3. Test responsive breakpoints:
   - Desktop (1920px): All grids display 3-4 columns
   - Tablet (768px): Grids adjust to 2 columns
   - Mobile (375px): Single column layout, no horizontal overflow
4. Verify:
   - Cards don't overflow container
   - Tables scroll horizontally on mobile
   - Buttons are touch-friendly (44px minimum)
   - Text remains readable at all sizes
   - Dark mode works correctly
5. Check all views:
   - Dashboard: Metrics grid responsive
   - Positions: Table scrollable on mobile
   - Signals: Grid responsive, filters accessible
   - Portfolio: Metrics grid responsive, chart placeholder visible
  </action>
  <verify>
    Manual verification of responsive layout in browser
  </verify>
  <done>Responsive design verified on all breakpoints</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 10: Verify production build completes successfully</name>
  <files>swing-trade-dashboard/dist/*</files>
  <action>
Verify production build completes successfully:

1. Run production build: cd swing-trade-dashboard && npm run build
2. Verify:
   - Build completes without errors
   - dist/ directory created
   - Minified JS files (less than 50KB gzipped)
   - CSS files minified
   - No console.log statements in build output
3. Preview production build: npm run preview
4. Verify all routes work:
   - / - Dashboard
   - /positions - Positions view
   - /signals - Signals view
   - /portfolio - Portfolio view
   - /nonexistent - 404 page
  </action>
  <verify>
    Build completes without errors, dist/ contains minified assets
  </verify>
  <done>Production build completes without errors and all routes verified</done>
</task>

</tasks>

<verification>
After all tasks complete, verify production readiness:

1. **Build verification:**
   - npm run build completes without errors
   - dist/ directory contains minified assets
   - Bundle size under 100KB (gzipped)

2. **Component verification:**
   - LoadingSpinner.vue renders correctly
   - ErrorMessage.vue displays error states
   - NotFoundView.vue shows 404 page

3. **Responsive verification:**
   - Desktop (1920px): 3-4 column grids
   - Tablet (768px): 2 column grids
   - Mobile (375px): Single column, scrollable tables

4. **Environment verification:**
   - .env.example has all required variables
   - Config.ts uses environment variables

5. **Router verification:**
   - All defined routes work
   - 404 page shown for unknown routes

6. **Dark mode verification:**
   - All components render correctly in dark mode
   - localStorage persistence works
</verification>

<success_criteria>
- [ ] LoadingSpinner.vue created with size variants and full screen option
- [ ] ErrorMessage.vue created with message, detail, retry functionality
- [ ] NotFoundView.vue created with 404 display and home link
- [ ] main.ts updated with error handler and production config
- [ ] vite.config.ts updated with minification and code splitting
- [ ] tsconfig.json updated with strict mode and proper module settings
- [ ] .env.example created with all required environment variables
- [ ] README.md updated with project documentation
- [ ] Production build completes without errors
- [ ] Responsive design verified on all breakpoints
- [ ] All views accessible and working in production build
</success_criteria>

<output>
After completion, create `.planning/phases/08-vue-dashboard/08-03-SUMMARY.md` documenting:
- Production components created
- Build configuration applied
- Environment configuration added
- Verification results
</output>
