---
phase: 08-vue-dashboard
plan: 02
type: execute
wave: 1
depends_on: ['08-01']
files_modified:
  - swing-trade-dashboard/src/components/MetricCard.vue
  - swing-trade-dashboard/src/components/PositionCard.vue
  - swing-trade-dashboard/src/components/SignalCard.vue
  - swing-trade-dashboard/src/components/PerformanceMetrics.vue
  - swing-trade-dashboard/src/views/DashboardView.vue
  - swing-trade-dashboard/src/views/PositionsView.vue
  - swing-trade-dashboard/src/views/SignalsView.vue
  - swing-trade-dashboard/src/views/PortfolioView.vue
  - swing-trade-dashboard/vitest.config.ts
  - swing-trade-dashboard/tests/unit/api/types.test.ts
  - swing-trade-dashboard/tests/unit/components/MetricCard.test.ts
  - swing-trade-dashboard/tests/unit/components/PositionCard.test.ts
  - swing-trade-dashboard/tests/unit/components/SignalCard.test.ts
autonomous: true
requirements: [DASH-03, DASH-04, DASH-05, DASH-06]
must_haves:
  truths:
    - "Dashboard shows system overview with real data from backend APIs"
    - "Positions view displays filterable list with search and status filtering"
    - "Signals view shows generated signals with filtering by type and confidence"
    - "Portfolio view displays performance metrics and trade history"
    - "All views handle empty states and data loading gracefully"
  artifacts:
    - path: "swing-trade-dashboard/src/components/MetricCard.vue"
      provides: "Reusable metric display card component"
      min_lines: 30
    - path: "swing-trade-dashboard/src/components/PositionCard.vue"
      provides: "Reusable position display card component"
      min_lines: 40
    - path: "swing-trade-dashboard/src/components/SignalCard.vue"
      provides: "Reusable signal display card component"
      min_lines: 40
    - path: "swing-trade-dashboard/src/components/PerformanceMetrics.vue"
      provides: "Performance metrics summary component with equity curve placeholder"
      min_lines: 50
    - path: "swing-trade-dashboard/src/views/DashboardView.vue"
      provides: "Main dashboard view with system overview"
      min_lines: 100
    - path: "swing-trade-dashboard/src/views/PortfolioView.vue"
      provides: "Portfolio performance view with metrics and trade history"
      min_lines: 120
    - path: "swing-trade-dashboard/vitest.config.ts"
      provides: "Vitest test configuration"
      contains: "defineConfig|vue|tsconfig|coverage"
    - path: "swing-trade-dashboard/tests/unit/api/types.test.ts"
      provides: "API types unit tests"
      min_lines: 30
    - path: "swing-trade-dashboard/tests/unit/components/MetricCard.test.ts"
      provides: "MetricCard component unit tests"
      min_lines: 50
    - path: "swing-trade-dashboard/tests/unit/components/PositionCard.test.ts"
      provides: "PositionCard component unit tests"
      min_lines: 50
    - path: "swing-trade-dashboard/tests/unit/components/SignalCard.test.ts"
      provides: "SignalCard component unit tests"
      min_lines: 50
  key_links:
    - from: "swing-trade-dashboard/src/views/DashboardView.vue"
      to: "swing-trade-dashboard/src/components/MetricCard.vue"
      via: "MetricCard component usage"
      pattern: "<MetricCard.*title="
    - from: "swing-trade-dashboard/src/views/PortfolioView.vue"
      to: "swing-trade-dashboard/src/components/PerformanceMetrics.vue"
      via: "PerformanceMetrics component usage"
      pattern: "<PerformanceMetrics.*portfolioSummary="

---

<objective>
Create reusable Vue components and enhance dashboard views for real-time monitoring of the swing trading system, with Vitest test infrastructure for component unit testing.

Purpose: Build the core monitoring interface with reusable components for metrics, positions, signals, and portfolio data. Each view should support filtering, search, and proper data display. Add Vitest test infrastructure to meet project's 80% coverage requirement.

Output: Complete dashboard with 4 views (Dashboard, Positions, Signals, Portfolio) using reusable card components with Tailwind styling and dark mode support, plus unit test coverage for components and API types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-vue-dashboard/08-01-SUMMARY.md
@.planning/phases/08-vue-dashboard/08-VUE-DASHBOARD-UI-SPEC.md
@.planning/phases/08-vue-dashboard/08-RESEARCH.md

# Existing Implementation Context

## API Types (from swing-trade-dashboard/src/api/types.ts)
```typescript
export interface Position {
  id: string
  symbol: string
  entryPrice: number
  entryDate: string
  quantity: number
  currentPrice: number
  status: 'OPEN' | 'CLOSED' | 'STOPPED' | 'TARGET_HIT'
  pnl: number
  pnlPercent: number
  stopLoss: number
  target: number
  reason: string
}

export interface Signal {
  id: string
  symbol: string
  signalType: 'BUY' | 'SELL' | 'HOLD'
  confidence: number
  reasoning: string
  entryPrice: number
  stopLoss: number
  target: number
  createdDate: string
  indicators: string[]
}

export interface PortfolioSummary {
  totalValue: number
  totalPnl: number
  totalPnlPercent: number
  winRate: number
  totalTrades: number
  averageWin: number
  averageLoss: number
  profitFactor: number
  maxDrawdown: number
  sharpeRatio: number
}

export interface MarketOverview {
  totalPositions: number
  openPositions: number
  totalValue: number
  todayPnl: number
}
```

## API Client (from swing-trade-dashboard/src/api/client.ts)
- getPositionList(): Promise<ApiResponse<Position[]>>
- getSignalList(params): Promise<ApiResponse<Signal[]>>
- getPortfolioSummary(): Promise<ApiResponse<PortfolioSummary>>
- getMarketOverview(): Promise<ApiResponse<MarketOverview>>
- getTradeHistory(limit): Promise<ApiResponse<Position[]>>
- generateSignals(): Promise<ApiResponse<void>>

## Project Testing Requirements (from CLAUDE.md)
- Minimum 80% test coverage
- Unit tests for individual functions, utilities, components
- Integration tests for API endpoints
- E2E tests for critical user flows (framework chosen per language)
- TDD workflow: Write tests first (RED), Implement to pass (GREEN), Refactor (IMPROVE)
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Vitest test infrastructure (Wave 0)</name>
  <files>swing-trade-dashboard/vitest.config.ts</files>
  <action>
Create vitest.config.ts for the Vue dashboard project:

1. Configuration structure:
   - defineConfig from 'vitest/config'
   - Vue plugin for Vite integration
   - Test directory: 'tests/unit'
   - File patterns: '**/*.test.ts', '**/*.spec.ts'

2. Coverage configuration:
   - provider: 'v8'
   - reporter: ['text', 'json', 'html']
   - exclude: ['node_modules/', 'tests/']

3. Environment options:
   - jsdom options for Vue component testing
   - globals: true for simpler test syntax

4. TypeScript support:
   - tsconfig path reference
   - coverage provider

5. Export as definedConfig
  </action>
  <verify>
    <automated>cd swing-trade-dashboard && grep -c "defineConfig\|coverage\|jsdom" vitest.config.ts</automated>
  </verify>
  <done>vitest.config.ts created with coverage, jsdom, and TypeScript support</done>
</task>

<task type="auto">
  <name>Task 1: Create API types unit tests (Wave 0)</name>
  <files>swing-trade-dashboard/tests/unit/api/types.test.ts</files>
  <action>
Create tests/unit/api/types.test.ts for API type validation:

1. Test file structure:
   - Import types from ../../src/api/types
   - Describe blocks for Position, Signal, PortfolioSummary, MarketOverview

2. Position type tests:
   - Valid position with all fields
   - Invalid position missing required fields
   - Status value validation

3. Signal type tests:
   - Valid signal with all fields
   - Signal type enum validation (BUY, SELL, HOLD)
   - Confidence range (0-100)

4. PortfolioSummary type tests:
   - Valid summary with all metrics
   - Required fields presence check

5. MarketOverview type tests:
   - Valid overview with current data
   - Optional fields handling

6. Run with: npx vitest run tests/unit/api/types.test.ts
  </action>
  <verify>
    <automated>cd swing-trade-dashboard && npx vitest run tests/unit/api/types.test.ts</automated>
  </verify>
  <done>API types unit tests created with 4 describe blocks and passing tests</done>
</task>

<task type="auto">
  <name>Task 2: Create MetricCard.vue unit tests</name>
  <files>swing-trade-dashboard/tests/unit/components/MetricCard.test.ts</files>
  <action>
Create tests/unit/components/MetricCard.test.ts for MetricCard component:

1. Component props tests:
   - Renders title prop correctly
   - Renders value prop correctly
   - Renders optional icon when provided
   - Renders trend indicator when trend prop provided
   - Hides trend when trend is null

2. Styling tests:
   - Card has rounded-2xl class
   - Dark mode class applied
   - Border classes present

3. Metric value display tests:
   - Green color for positive values
   - Red color for negative values
   - Neutral color for zero values

4. Component mounting:
   - Use @vue/test-utils mount
   - Verify DOM contains expected elements
   - Test with various prop combinations

5. Run with: npx vitest run tests/unit/components/MetricCard.test.ts
  </action>
  <verify>
    <automated>cd swing-trade-dashboard && npx vitest run tests/unit/components/MetricCard.test.ts</automated>
  </verify>
  <done>MetricCard component tests created with 5 describe blocks and passing tests</done>
</task>

<task type="auto">
  <name>Task 3: Create PositionCard.vue unit tests</name>
  <files>swing-trade-dashboard/tests/unit/components/PositionCard.test.ts</files>
  <action>
Create tests/unit/components/PositionCard.test.ts for PositionCard component:

1. Component props tests:
   - Renders position symbol correctly
   - Displays entry price from position prop
   - Shows current price
   - Calculates and displays P&L percentage

2. Status badge tests:
   - OPEN status shows green badge
   - CLOSED status shows red badge
   - STOPPED status shows orange badge
   - TARGET_HIT status shows red badge

3. Action button tests:
   - Close button shows for OPEN positions
   - Close button hidden for CLOSED positions
   - Close button emits closePosition event

4. Dark mode tests:
   - Dark mode classes applied correctly

5. Component mounting:
   - Use @vue/test-utils mount with Pinia store if needed
   - Verify all required fields displayed

6. Run with: npx vitest run tests/unit/components/PositionCard.test.ts
  </action>
  <verify>
    <automated>cd swing-trade-dashboard && npx vitest run tests/unit/components/PositionCard.test.ts</automated>
  </verify>
  <done>PositionCard component tests created with 5 describe blocks and passing tests</done>
</task>

<task type="auto">
  <name>Task 4: Create SignalCard.vue unit tests</name>
  <files>swing-trade-dashboard/tests/unit/components/SignalCard.test.ts</files>
  <action>
Create tests/unit/components/SignalCard.test.ts for SignalCard component:

1. Component props tests:
   - Renders signal type badge correctly
   - BUY badge is green
   - SELL badge is red
   - HOLD badge is gray

2. Signal details tests:
   - Displays symbol correctly
   - Shows confidence percentage
   - Renders entry, stop loss, target prices
   - Displays reasoning text

3. Indicators tests:
   - Renders indicators array as badges
   - Empty indicators handled gracefully

4. Empty state tests:
   - Shows message when reasoning is empty

5. Dark mode tests:
   - Dark mode classes applied correctly

6. Component mounting:
   - Use @vue/test-utils mount
   - Verify all elements rendered

7. Run with: npx vitest run tests/unit/components/SignalCard.test.ts
  </action>
  <verify>
    <automated>cd swing-trade-dashboard && npx vitest run tests/unit/components/SignalCard.test.ts</automated>
  </verify>
  <done>SignalCard component tests created with 5 describe blocks and passing tests</done>
</task>

<task type="auto">
  <name>Task 5: Update DashboardView.vue - Enhance with real data loading</name>
  <files>swing-trade-dashboard/src/views/DashboardView.vue</files>
  <action>
Update DashboardView.vue to properly load and display real data:

1. Keep existing 6 metric cards:
   - Total Positions, Total Value, Today's P&L, Win Rate, Total Trades, Total P&L

2. Add proper data loading with error handling:
   - onMounted hook with Promise.all for parallel API calls
   - getMarketOverview(), getPortfolioSummary(), getPositionList()
   - Handle response.success check before setting refs

3. Recent positions table:
   - Keep existing table structure
   - Limit to 5 positions: positionsRes.data.slice(0, 5)
   - Empty state: "No recent positions"

4. Responsive layout:
   - Grid: grid-cols-1 md:grid-cols-2 xl:grid-cols-3
   - Table: overflow-x-auto wrapper
   - Proper padding with p-4 and mb-6 spacing

5. Add error boundary: Try-catch around API calls with console.error
  </action>
  <verify>
    <automated>grep -c "onMounted\|getMarketOverview\|getPortfolioSummary" swing-trade-dashboard/src/views/DashboardView.vue</automated>
  </verify>
  <done>DashboardView.vue loads market overview, portfolio summary, recent positions with proper error handling</done>
</task>

<task type="auto">
  <name>Task 6: Update PositionsView.vue - Enhance with filtering and close functionality</name>
  <files>swing-trade-dashboard/src/views/PositionsView.vue</files>
  <action>
Update PositionsView.vue to add filtering and close position functionality:

1. Keep existing filters:
   - Search input for symbol
   - Status dropdown (OPEN, CLOSED, STOPPED, TARGET_HIT)

2. Positions table:
   - Keep existing columns: Symbol, Entry Date, Entry Price, Quantity, Current Price, P&L %, Status, Actions
   - Filtered positions: computed with lowercase search
   - Empty state: "No positions found"

3. Close position workflow:
   - Open modal with positionToClose ref
   - Confirmation modal with dark mode
   - closePosition() API call
   - Refresh positions after close
   - Reset positionToClose

4. Action button:
   - Only show for OPEN positions
   - Indigo-600 button with hover:bg-indigo-700

5. Empty state for no filtered results
  </action>
  <verify>
    <automated>grep -c "filteredPositions\|closePosition" swing-trade-dashboard/src/views/PositionsView.vue</automated>
  </verify>
  <done>PositionsView.vue has symbol search, status filter, close position confirmation modal</done>
</task>

<task type="auto">
  <name>Task 7: Update SignalsView.vue - Enhance with signal generation and filtering</name>
  <files>swing-trade-dashboard/src/views/SignalsView.vue</files>
  <action>
Update SignalsView.vue to add signal generation and filtering:

1. Keep existing filters:
   - Signal type dropdown (All, BUY, SELL, HOLD)
   - Confidence filter (minConfidence input)

2. Generate signals button:
   - Top-right button (indigo-600, px-6 py-2.5)
   - Calls generateSignals() API endpoint
   - Refreshes signals after generation

3. Signals grid:
   - 3-column grid: sm:grid-cols-2 xl:grid-cols-3
   - SignalCard component usage (refactor to use component)
   - Signal type badges with colors
   - Confidence display as percentage

4. Empty state:
   - "No signals found" message
   - "Generate signals now" button

5. Error handling: Try-catch around API calls
  </action>
  <verify>
    <automated>grep -c "generateSignals\|filteredSignals" swing-trade-dashboard/src/views/SignalsView.vue</automated>
  </verify>
  <done>SignalsView.vue has signal type filter, confidence filter, generate signals button</done>
</task>

<task type="auto">
  <name>Task 8: Update PortfolioView.vue - Enhance with performance metrics and trade history</name>
  <files>swing-trade-dashboard/src/views/PortfolioView.vue</files>
  <action>
Update PortfolioView.vue to display performance metrics and trade history:

1. Keep existing portfolio summary cards:
   - Total P&L, Win Rate, Total Trades, Profit Factor

2. Equity curve placeholder:
   - h-64 with dashed border
   - Chart icon
   - "Equity Curve Chart" label
   - Empty state if no data

3. Trade history table:
   - Keep existing columns: Date, Symbol, Type, Entry, Exit, P&L, Duration
   - Time range dropdown: 1W, 1M, 3M, 6M, 1Y, ALL
   - Empty state: "No trade history yet"

4. Status badge for trades:
   - OPEN: success-600 badge
   - CLOSED: error-600 badge

5. Dark mode for all elements
  </action>
  <verify>
    <automated>grep -c "getPortfolioSummary\|getTradeHistory" swing-trade-dashboard/src/views/PortfolioView.vue</automated>
  </verify>
  <done>PortfolioView.vue displays 4 performance metrics, equity curve placeholder, trade history table</done>
</task>

</tasks>

<verification>
After all tasks complete, verify the dashboard:

1. **Component files exist:**
   - MetricCard.vue, PositionCard.vue, SignalCard.vue, PerformanceMetrics.vue
   - All use Vue 3 SFC format with defineProps

2. **View files have proper data loading:**
   - DashboardView.vue: onMounted with Promise.all
   - PositionsView.vue: filteredPositions computed with search
   - SignalsView.vue: filteredSignals with type and confidence
   - PortfolioView.vue: portfolioSummary and recentTrades

3. **Test infrastructure:**
   - vitest.config.ts exists with coverage config
   - API types tests pass: npx vitest run tests/unit/api/types.test.ts
   - Component tests pass: npx vitest run tests/unit/components/

4. **Dark mode support:**
   - All components use dark:bg-white/[0.03], dark:border-gray-800
   - Dark text classes: dark:text-white, dark:text-gray-300

5. **Responsive grid:**
   - Metrics grid: md:grid-cols-2 xl:grid-cols-3
   - Signals grid: sm:grid-cols-2 xl:grid-cols-3

6. **Compilation:**
   - cd swing-trade-dashboard && npm run dev succeeds
   - No TypeScript errors
</verification>

<success_criteria>
- [ ] vitest.config.ts created with coverage and jsdom support
- [ ] tests/unit/api/types.test.ts created with passing tests
- [ ] tests/unit/components/MetricCard.test.ts created with passing tests
- [ ] tests/unit/components/PositionCard.test.ts created with passing tests
- [ ] tests/unit/components/SignalCard.test.ts created with passing tests
- [ ] MetricCard.vue created with title, value, icon, trend props
- [ ] PositionCard.vue created with position prop, status badges, close modal
- [ ] SignalCard.vue created with signal type badges, confidence display
- [ ] PerformanceMetrics.vue created with metrics grid, equity curve placeholder
- [ ] DashboardView.vue loads market overview and portfolio summary
- [ ] PositionsView.vue has symbol search and status filter
- [ ] SignalsView.vue has signal type and confidence filter
- [ ] PortfolioView.vue displays performance metrics and trade history
- [ ] All components have dark mode support
- [ ] All views handle empty states appropriately
- [ ] Vue dev server starts without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-vue-dashboard/08-02-SUMMARY.md` documenting:
- All reusable components created
- View enhancements implemented
- API integration verified
- Dark mode support confirmed
- Vitest test infrastructure added with component unit tests
- Test coverage results
</output>
